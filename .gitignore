# Dataset and generated artifacts
/data/
/datasets/
/open/train_unlabeled.jsonl
/artifacts/
/runs/
# 디렉터리째 빼면 하위 파일을 되살릴 수 없어 내용물만 뺀다.
# 팀 배정의 후보 소스는 산출물이 아니라 커밋 대상이다 (docs/tasks/team-handoff.md:196-197).
/experiments/*
!/experiments/sme_candidate.py
!/experiments/qualification_candidate.py
/checkpoints/
/weights/
/predictions/
/registry/
/submissions/
/final-models/

# Model files
*.pt
*.pth
*.ckpt
*.onnx

# TensorBoard and training logs
events.out.tfevents.*
/tensorboard/
/logs/

# Secrets and credentials
.env
.env.*
!.env.example
.aws/
**/.aws/
.aws-sam/
.aws-sso/
.kaggle/
credentials/
*credentials*.json
*service-account*.json
*.pem
*.key

# Python environments and caches
.venv/
venv/
env/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE and operating-system files
.idea/
.vscode/
.DS_Store
Thumbs.db

# Machine-local agent hooks and generated wiki state
/.claude/settings.json
/.claude/settings.local.json
/.codex/hooks.json
/.wiki/*.json
/.wiki/*.jsonl
/.wiki/.sync

# Competition run outputs (keep manifests and reports intentionally)
/output/
/open/output/
