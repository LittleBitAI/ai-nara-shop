# A3 — F1 0.000 인 셋을 세운다 (v10 · v18 · v20)

담당: `glassfish` 세션(astra). 배정 2026-09-20.
이 작업서가 입력·기준선·수정 범위·통과 조건을 소유한다.

## 왜 이것인가

현재 dev Macro F1 **0.524626**(`c0daca5`, 재생 기준). 남은 천장을 항목별로 세면
**이 셋이 가장 크다 — 합계 +0.125** 다. 그리고 셋 다 같은 이유로 막혀 있다.

| 항목 | 공식 항목명 | F1 | TP/FP/FN | 양성 | Macro 몫 |
| --- | --- | ---: | --- | ---: | ---: |
| v10 | (`docs/items.md` 확인) | **0.000** | 0/0/7 | 7 | +0.0417 |
| v18 | 소기업 제한 없음 | **0.000** | 0/2/7 | 7 | +0.0417 |
| v20 | 소프트웨어 | **0.000** | 0/0/5 | 5 | +0.0417 |

셋 다 **부재탐지**다 — 스키마가 `근거문구`를 null 로 고정하므로 인용으로 못 고친다(D4-5).

## 확정된 사실 — 모델이 아예 안 선다

회차 `colab-1789880471715651259`(`0a8459a`)의 **1차 호출 원응답 200건**을 그대로 세었다.

| 항목 | 모델 1차가 `위반여부=1` 이라 한 건수 (200건 중) | 그 중 양성 적중 |
| --- | ---: | ---: |
| v10 | **0** | 0 |
| v18 | **0** | 0 |
| v20 | **0** | 0 |

**후처리가 죽이는 것이 아니다.** 24항목 합동 프롬프트에서 모델이 이 셋을 한 번도 1로
쓰지 않는다. 그러므로 후처리·재생으로 고칠 자리가 없고, **GPU 회차가 필요하다.**
이것이 이 작업을 `glassfish` 로 보내는 이유다.

### 전례가 있다

같은 모양을 이미 한 번 뚫었다. v16 이 여섯 회차 연속 TP=0 이었는데, 24항목 합동 대신
**부재탐지 3항목만 별도 스키마로 물었더니 양성 6건을 전부 잡았다**
(`.wiki/decisions/2026-09-18-run-colab-1789658250172468461.md`).
원인은 모델의 무지가 아니라 **출력 형식**이었다.

그런데 그 다음 회차에서 판정 칸을 하나로 합쳤더니 재현율이 13/18 → 3/18 로 떨어졌다
(`.wiki/decisions/2026-09-18-run-colab-1789695980726180378.md`). **어떻게 묻느냐가
그대로 점수다.** 느슨하게 물으면 숫자가 오르지만 그건 실력이 아니다.

## 각 항목에서 이미 확인된 것 — 다시 파지 마라

### v20 (소프트웨어) — 부재탐지의 대상 문자열이 아예 없다

「중소 소프트웨어사업자의 사업 참여 지원에 관한 지침」 제3조 ②가 입찰공고문에
"**대기업 참여제한 하한제도 적용 여부(적용 근거 포함)를 명시하여야 한다**"고 요구한다.
부재탐지의 대상으로 딱 맞아 보인다. 그런데:

| 확인한 것 | 결과 |
| --- | --- |
| `대기업 참여제한` 문자열 | **dev 200건 전체에 0건.** 양성에도 음성에도 없다 |
| `소프트웨어` 낱말 | 39건에 있고 **양성 5건을 전부 덮는다** — 게이트 후보 |
| `정보화` 낱말 | 4건, 양성 2/5 |
| `meta.정보화사업여부` | 양성 5건이 **전부 "미입력"** |
| 금액 | 1.24억~49.5억으로 흩어져 별표1의 20/40/80억 구간과 대응 안 됨 |

`소프트웨어` 39건이 양성 5건을 전부 덮는 것이 **유일하게 쓸 만한 신호**다.

### v18 (소기업 제한 없음) — 낱말로는 못 가른다

| 낱말 | 200건 중 | 그 중 양성 |
| --- | ---: | ---: |
| `소기업` | 179 | 4/7 |
| `소상공인` | 144 | 4/7 |

거의 모든 공고에 있어서 게이트가 안 된다.
astra 가 `reports/team-c/a1-company-size/followup-analysis.md` 에서 FN 7건의 차단 사유를
이미 표로 냈다. 다만 **그 표는 `369d4db` 기준이고 그 뒤 두 가지가 바뀌었다** —
`scope` 절 수정(`0a8459a`)과 인용 공백 복원(`c0daca5`). 공백 차단 5건은 이제 수리됐고,
v18 은 그래도 0.000 이다. **차단 사유를 최신 회차로 다시 세는 것이 첫 걸음이다.**

### v10 — 경쟁제품 게이트는 이제 열려 있다

`scope` 절 수정으로 모델의 `scope == "competitive"` 가 **0건 → 15/15** 가 됐다
(`reports/team-c/merged-candidate/result.md`). v10·v11·v13 양성 합집합 15건이 전부 열렸다.

**그런데 v10 은 한 셀도 안 움직였다.** `facts["scope"]` 는 `script.py:538` 한 자리에서만
읽히고 그건 v14~v18 전용이다. **그 배선은 이 작업이 아니다 — 주 세션이 맡는다.**
이 작업은 게이트가 열린 뒤 **무엇을 판정할 것인가**다.

낱말 신호: `중소기업자간 경쟁제품` 11건(양성 0/7), `직접생산` 72건(양성 4/7).

## 입력

- 코드: `main` 의 `c0daca5` 이후. 이 커밋을 고정하고 시작한다.
- 원응답: `reports/runs/colab-1789880471715651259/dev-debug/diagnostics.jsonl` (200건)
- 정답: `open/dev_labels.csv` · 입력: `open/dev.jsonl`
- 항목 정의·조문: `docs/items.md` 와 `open/data/항목표.json`.
  **번호의 뜻을 추측하지 않는다.**

## 수정 범위

`script.py` 의 프롬프트·스키마·별도 호출 단계, 이 작업서, `docs/tasks.md`, 보고서.
**다른 항목의 판정 로직·A2 정규식·`verify_company_size` 결정표는 건드리지 않는다.**
한 회차에 한 가설만 넣는다.

## 통과 조건

1. **대상 밖 21항목의 변화를 `tools/compare_runs.py` 로 따로 낸다.** 프롬프트 변경이라
   0셀을 요구할 수 없으므로, 과거 회차 간 실측 churn 17~45셀과 비교해 보고한다.
2. v10·v18·v20 각각의 **TP 가 실제로 0을 넘는지**. 발화 건수 증가만으로 채택하지 않는다.
3. **느슨하게 물어서 오른 것이 아님을 보인다.** 판정 칸을 합치거나 조건을 흐리면
   재현율이 오르고 정밀도가 무너진다 — 위 2026-09-18 두 회차가 그 증거다.
   FP 도 함께 보고한다.
4. **무라벨 6,000건 발화율(W5).** 새 규칙·새 호출의 발화 건수·비율·dev 대비 배율.
   모델 출력이 필요하면 별도 회차다.
5. **시간 예산.** 별도 호출을 늘리면 건당 호출 수가 는다. 지금 **2.52회/건**이고
   dev-debug 가 4.43초/건이다. 1,853건 환산이 8,209초로 **이미 한도 7,200초를 넘는다**
   (마지막 서버 실측은 2.31초/건이었다). **호출을 늘리는 설계는 그 대가를 같이 잰다.**

## 규칙

- 같은 ZIP 재실행으로 그날 그 코드의 churn 을 잰다. 한 회차의 `dev` 와 `dev-debug` 로
  대신하지 않는다 — 이번 회차에서 그 둘이 0.006932 벌어졌다.
- **제출하지 않는다.** dev 0.70 전에는 점수 목적의 제출을 하지 않는다.
- 판정에는 대회 제공 자료만 쓴다. dev 정답은 발견과 사후 채점에만 쓰고 규칙의 조건으로
  쓰지 않는다.
- 회차 실행법은 `reports/team-c/merged-candidate/run-request.md`. `REPO_REF` 는
  **40자 전체 SHA** 여야 한다(약칭은 `fatal: couldn't find remote ref` 로 죽는다).

## 결과

보고서를 `reports/team-c/a3-zero-items/` 에 남기고 여기에 링크한다.
