# 9/26~9/29 일일 실행 계획 — 3인(A+B · C · D)

작업 ID / 제목: `plan-0926-0929` / astra 의 [0.72 실행안](../../reports/server-gap-20260925/result.md)을 일자·담당별 작업으로 푼다
담당 역할 / 담당자: A+B = 본인, C = 팀원 2, D = 팀원 3. B(팀원 1)는 9/25 이탈
상태: ready
작성: 2026-09-25 22:40, 기준 코드 `4a15529`(서버 0.5930337159), 마감 9/29 10:00

이 문서는 계획이다. 적힌 숫자 가운데 실측은 과거 기록에서 옮긴 것뿐이고, 비용·시간 추정은 시범 20건으로 다시 잰다.

## 정한 것 (2026-09-25 사용자 답변)

| 물음 | 결정 |
| --- | --- |
| 가동 | C·D 하루 종일, 본인 하루 10시간 이상. GPU 회차 하루 4회 이상 |
| 분담 | C = 경쟁제품 사실 정밀화 → few-shot. D = 라벨 600 · 라벨러 dev 보정 · GPU 회귀. A+B = 문단 선택 · 선택적 재검증 · v9/v24 · 통합 · 제출 |
| 라벨 | 새 600건(봉인 400 + 진단 200). 24항목을 라벨하고 dev 보정을 통과한 항목만 보고. 처음엔 Sonnet 5 + GPT-6 Luna 였으나 9/26 Luna(API, 캐싱, effort high) 단독으로 바꿨다 — [labels-600](labels-600.md) |
| 가설 | 네 가지 모두: 문서 내부 문단 선택 · 경쟁제품 사실 정밀화 · 검증된 사례 few-shot · 선택적 재검증 |
| 선택 회차 | N3(경쟁제품)와 N1(v16·v18 분할)을 9/26 에 돌린다. 그대로 싣지 않고 선택적 재검증의 재료로 쓴다 |
| 시간 예산 | 문단 선택은 토큰 상한 불변. 서버 총시간 목표 6,800초, 남은 약 440초를 few-shot·재검증이 나눈다 |
| 채택 규칙 | 아래 [채택 규칙](#채택-규칙--둘-중-하나로-가른다) |
| 리뷰 | W5 그대로. 가설은 회차 먼저, 효과가 보이면 리뷰. 운영 `script.py` 통합만 회차 전에 리뷰 |
| 제출 칸 | 매일 준비된 최선 후보. 이긴 것이 없으면 `4a15529` 를 다시 올려 서버 변동폭을 잰다 |
| 9/29 | 검증된 새 후보, 아니면 서버 최고 코드 |

## 하루 리듬

| 시각 | 무엇 | 누가 |
| --- | --- | --- |
| 10:00 | 시작 확인 — 전날 서버 점수를 장부에 적고 그날 가설·회차를 배정한다 | 셋 |
| 19:00 | 통합 마감 — 이긴 후보 PR 을 A+B 에게 넘긴다. 이후 들어온 것은 다음 날 몫 | C·D → A+B |
| 19:00~20:00 | 통합 브랜치 ZIP 을 GPU 두 회차로 동시에 돌린다(Colab 두 세션) | D |
| 20:00 | 동결 — `tools/package.py` 로 ZIP, `script.py` SHA-256 을 장부 후보에 적는다 | A+B |
| 21:00 | 업로드 | A+B |
| 22:30 | 서버 점수 공유, [submissions.json](../../reports/submissions.json) 에 적고 다음 날 계획을 고친다 | A+B |

서버 실행은 약 1시간 47분이고 대기열로 더 늦어질 수 있다(`docs/contest.md`). 22:30 에 점수가 없으면 다음 날 10:00 에 본다.

## 채택 규칙 — 둘 중 하나로 가른다

후보는 아래를 모두 만족하면 채택, 하나라도 못 하면 반려한다. 보류는 없다.

1. 같은 ZIP 의 GPU 두 회차 모두에서 대상 항목의 `(TP − FP)` 순이득 > 0. `tools/compare_runs.py --items <대상>` 로 본다.
   후처리만 바꾼 후보는 GPU 대신 다섯 원응답 재생으로 잰다.
2. 대상 밖 회귀 셀이 두 회차 각각 2셀 이하.
3. 봉인 400 의 신뢰 항목에서 기준(`4a15529`) 대비 악화 없음. 봉인 라벨이 준비되기 전(9/26)은 1·2·4 만 본다.
4. 서버 총시간 추정 ≤ 6,800초. `서버 총초 ≈ (dev 추론 초 ÷ 200) × 1853 × 0.99` 로 잰다.

채택된 후보도 `dev 라벨만` 규칙이면 PR 에 그 사실과 무라벨 발화 배율을 적는다(확정 7).

## 라벨 600건 설계 (D)

### 표본 — 라벨을 열기 전에 고정한다

| 집합 | 크기 | 뽑는 곳 | 용도 |
| --- | ---: | --- | --- |
| 봉인 | 400 | 무라벨 20,000 에서 이미 라벨을 연 공고(40 · B12 300 · 무라벨 D 회차 6,000)와 그 본문 중복을 빼고 seed `20260926` 무작위 | 후보를 고정한 뒤에만 연다. 신뢰 항목 F1 보고 |
| 진단 | 200 | 무라벨 D 회차 6,000건 중 약한 항목(v2·v9·v10·v11·v13·v16·v18·v24) 발화 공고 우선 | 원응답이 있어 후처리 재생이 된다. few-shot 예시와 오류 분석 |

- `tools/unlabeled_sample.py` 는 seed 가 상수(`20260923`)다. `--seed` 인자 하나만 추가해 재사용한다.
- 봉인 400 은 ID 목록과 manifest(입력 SHA-256 · seed · 제외 집합)를 커밋해 해시로 고정한다. 라벨 파일은 커밋하되 후보 개발 중에는 열지 않는다.
- 진단 200 은 무작위가 아니다. 이 집합의 F1 은 서버 대리값이 아니다(양성 비율이 다르다).

### 라벨러

| | Sonnet 5 | GPT-6 Luna |
| --- | --- | --- |
| 실행 | `claude -p --model claude-sonnet-5`, 도구 없음, 본인 PC·본인 계정 | OpenAI API `gpt-6-luna`, reasoning effort high |
| 프롬프트 | `tools/label_bundle.py export --law-excerpt <발췌>` 의 인라인 조문 모드, 24항목, 두 모델 같은 `prompt.md` | 같은 `prompt.md` |
| 캐싱 | `claude -p` 는 호출마다 전체를 캐시 기록한다(B12 기록: 쓰기 약 19k · 읽기 0) | 고정 접두부(지시 · 항목표 · 조문 발췌)를 앞에, 공고 본문을 뒤에 둔다. `prompt_cache_key` 를 고정한다 |
| 러너 | `label_bundle.py run --cmd` 그대로 | `--cmd` 에 붙일 얇은 CLI 하나(stdin 프롬프트 → Responses API → JSON stdout, `usage` 기록) |

R6·R15 에 따라 두 러너 모두 명령·모델 ID·프롬프트 SHA-256·원응답·`usage` 를 manifest 에 남긴다.
입력은 제공 자료뿐이고 웹 검색은 끈다. 제출 추론 경로에는 들어가지 않는다(R7).

### 보정 규칙 — 결과 보기 전에 고정

항목마다 두 라벨러 각각의 dev 200 F1 로 정한다([dev200 보정](../../reports/label-compare/dev200/README.md)과 같은 문턱).

| 라벨러 F1 | 판정 |
| --- | --- |
| 0.80 이상 | 신뢰 — 봉인 평가에 쓴다 |
| 0.60~0.80 | 조건부 — 두 라벨러가 일치한 셀만 쓰고, 불일치 셀은 D 가 원문을 읽는다 |
| 0.60 미만 | 제외 — 이 항목은 봉인 평가에서 보고하지 않는다 |

두 라벨러가 모두 신뢰인 항목은 일치 셀을 정답으로 두고 불일치 셀만 D 가 원문 확인한다. 한쪽만 신뢰면 그쪽 라벨을 쓴다.
과거 Opus 5.5 라벨러는 v10·v18·v20 F1 0, 약한 여덟 항목 대부분이 제외였다. 이번에도 약한 항목이 제외되면 봉인 400 은 그 항목의 후보를 가르지 못한다 — 그 항목은 GPU 두 회차만으로 판정한다.

### 토큰·비용 추정

과거 기록에서 옮긴 것:

| 기록 | 모델·방식 | 공고당 |
| --- | --- | --- |
| B11 dev 200 ×2 | Sonnet 5, 1턴, 4항목 | 캐시 쓰기 약 21k, 출력 약 0.5k, API 환산 $0.088 |
| B12 무라벨 300 | Sonnet 5, 1턴, v19 | 캐시 쓰기 약 19.4k, 출력 약 0.8k, $0.086. 269건 뒤 할당량 소진 |
| dev200 24항목 | Opus 5.5 medium, 도구 사용 | 약 37초. 토큰 기록 없음 |

이번 24항목 1턴은 공고당 입력 약 24k(본문 약 20k + 고정 접두부 약 4~6k), 출력 약 3~5k 로 잡는다.

Sonnet 5(구독, 본인 $100 계정): 800호출 × 약 28k = 약 22M 토큰, API 환산 호출당 약 $0.15 · 합계 약 $120.
B12 속도로 $20 요금제 한 창이 약 130~150호출이니 $100 요금제에서 1~2창이다. 본인 개발 세션과 한도를 나눠 쓴다.

GPT-6 Luna(API, 공식 단가 입력 $0.10/M · 캐시 입력 $0.01/M · 캐시 쓰기 $0.125/M · 출력 $0.50/M, Batch 50%):

| 부분 | 토큰/호출 | 단가 | 호출당 |
| --- | ---: | --- | ---: |
| 고정 접두부(캐시 적중) | 약 5k | $0.01/M | $0.00005 |
| 공고 본문(캐시 안 됨, 쓰기로 과금돼도) | 약 20k | $0.10~0.125/M | $0.002~0.0025 |
| 출력 + reasoning(high) | 약 4k + 10~30k | $0.50/M | $0.007~0.017 |
| 합계 | | | 약 $0.009~0.020 |

800호출 약 $7~16. 상한 $30 안이다. 비용의 대부분은 출력·reasoning 이고, 공고 본문은 호출마다 달라 캐시가 줄이는 것은 접두부 몇 천 토큰뿐이다(호출당 $0.0005 안팎).
멈춤선: 시범 20건의 실측 평균 × 800 이 $30 을 넘으면 effort 를 medium 으로 내리고, 그래도 넘으면 진단 200 을 Sonnet 단독으로 돌린다.
봉인·진단 600 은 밤새 돌려도 되므로 Batch(50%)를 쓸 수 있다. dev 보정 200 은 결과가 급하니 Standard 로 돌린다.

## 일일 계획

### 9/25 밤 (지금 ~ 01:00)

| 담당 | 할 일 | 끝의 모습 |
| --- | --- | --- |
| A+B | 이 계획서·`plan-active`·`team-handoff` 커밋 | C·D 가 내일 10:00 에 읽을 문서가 `main` 에 있다 |
| A+B | `4a15529` 의 GPU 기준선 두 회차를 걸어 둔다(`RUN_DIAGNOSTIC=True`) | 제출 코드 자체의 dev 두 값과 원응답 두 벌. 내일 모든 재생·비교의 기준 |
| A+B | OpenAI API 키·월 상한 $30 설정, `gpt-6-luna` 한 번 호출로 키 확인 | D 가 내일 오전에 바로 시범을 돌릴 수 있다 |

### 9/26 (토) — 측정하는 날

| 담당 | 오전 | 오후 | 19:00 까지 넘길 것 |
| --- | --- | --- | --- |
| A+B | 문단 선택 발동 감사: dev 정답의 근거문구(e1~e24)가 지금 `build_context(max_chars=16000)` 안에 있는지 셀마다 센다. 약한 항목의 FN 셀 중 근거가 문맥 밖인 수를 항목별로 낸다 | 감사가 통과하면 `run/a-context-select` 에 키워드 창 선택(같은 16,000자 상한 · 같은 호출 수)을 켜고 GPU 두 회차 | 통합·패키징. 9/26 제출은 이긴 후처리만, 없으면 `4a15529` 재제출 |
| C | N3 GPU 두 회차(`run/a-n3-product`) 걸기. 기다리는 동안 다섯 원응답에서 v10·v11·v13 FP/FN 셀을 품목 코드·행 조건·자격 주체 오류로 분해 | 제공 CSV(`중기부고시_경쟁제품_세부품명.csv`) 정확 코드 조회 후처리 후보 → 다섯 원응답 재생 → 이기면 GPU 두 회차 | 이긴 후처리 PR |
| D | 봉인 400·진단 200 ID 고정·커밋. 번들 export, Luna CLI 작성, 두 라벨러 시범 dev 20건 → 실측 토큰·비용으로 위 추정 갱신 | dev 200 보정 두 라벨러(Sonnet 은 본인 PC). N1 GPU 두 회차(`run/a-n1-split`) 걸기 | 항목별 신뢰 표. 밤에 무라벨 600 라벨링 시작 |

문단 선택 감사의 멈춤선(결과 전에 고정): 약한 여덟 항목 FN 셀 가운데 근거가 문맥 밖인 셀이 5셀 미만이면 문단 선택을 반려하고 오후는 선택적 재검증 설계로 옮긴다.
부재탐지 항목(v10·v11·v16·v18·v20)은 정답 근거문구가 비어 있어 이 감사로 못 센다.

### 9/27 (일) — 결합하는 날

| 담당 | 오전 | 오후 | 19:00 까지 넘길 것 |
| --- | --- | --- | --- |
| A+B | 9/26 서버 점수 장부. 문단 선택 두 회차 판정(규칙 1·2·4). 이겼으면 봉인 400 GPU 기준·후보 각 1회(약 25분씩) | N1·N3 결과로 선택적 재검증 조건 설계 — 기존 추가 호출을 대체하거나 문턱 근처 셀만 다시 묻는다. 시간식으로 약 440초 안인지 먼저 계산 → GPU 두 회차. v24 FN 셀 분석(CPU) | 통합 브랜치 |
| C | N1·N3 판정 정리(v10·v11·v12·v16·v18 TP/FP/FN 전→후)를 A+B 에게 | few-shot: 진단 200 과 dev(자기 자신 제외)에서 같은 법적 경계의 양성·음성 1~2쌍. 예시 토큰이 시간 예산을 먹으므로 먼저 초/건을 잰다 → GPU 두 회차 | 이긴 후보 PR |
| D | 무라벨 600 라벨 회수. 신뢰 항목 불일치 셀 원문 확인(상한 150셀) | 봉인 400 정답 확정·해시 커밋. 기준 `4a15529` 의 봉인 400 GPU 1회. 진단 200 을 C·A+B 에게 공개 | 19:00~20:00 통합 ZIP GPU 두 회차 |

### 9/28 (월) — 통합하는 날, 새 가설 없음

12:00 이후 새 가설을 받지 않는다. 오후는 이긴 것을 모으고 재는 일만 한다.

| 담당 | 오전 | 오후 | 20:00 |
| --- | --- | --- | --- |
| A+B | 9/27 점수 장부. 이긴 변경만 통합 PR(운영 `script.py` 통합이라 회차 전 리뷰). `docs/contest.md` 의 Private Score 가 최고점인지 마지막 제출인지 확인 | 다섯 원응답 재생 · 봉인 400 신뢰 항목 · 시간 추정 ≤ 6,800초 | 최종 후보 동결 · 21:00 업로드 |
| C | 리뷰 지적 수리만 | 무라벨 발화 배율을 PR 에 적는다(W5) | — |
| D | 통합 ZIP GPU 두 회차, 봉인 400 GPU 1회 | `docs/runs.md` 등록, 라벨 생성 이력(R15) 정리 | — |

### 9/29 (화) 오전 — 마지막 칸

서버 실행이 약 1시간 47분이고 대기가 붙을 수 있으니 07:00 까지 올린다.

1. 06:30 — 9/28 서버 점수 확인.
2. 9/28 밤에 채택 규칙을 통과한 새 변경이 있으면 그 `main`, 없으면 서버 최고 코드를 올린다.
3. Private Score 가 마지막 제출이면 이 칸이 최종 점수다. 최고점이면 새 후보가 없을 때 칸을 쓰지 않아도 된다.

## 멈춤선 모음

| 조건 | 그때 할 일 |
| --- | --- |
| Luna 시범 20건 비용 × 800 > $30 | effort medium → 그래도 넘으면 진단 200 은 Sonnet 단독 |
| Sonnet 할당량 소진 | 창이 초기화될 때까지 Luna 만 진행. 봉인 400 을 먼저, 진단 200 을 나중에 |
| 두 라벨러 모두 약한 항목 제외 | 그 항목은 GPU 두 회차만으로 판정. 봉인 평가 보고에서 뺀다 |
| 후보가 서버 추정 6,800초 초과 | 반려. 호출 조건을 좁혀 다시 잰다 |
| 9/28 12:00 이후 새 가설 | 받지 않는다 |

## 건드리지 않는 것

- [기각·보류된 가설](../../.wiki/plan-active.md#기각보류된-가설--다시-열지-않는다) 표의 방향 — 법령 추가 주입, v2 프롬프트, thinking 모드.
- #143 과 기록 브랜치 넷은 이번 계획 밖이다. 9/29 뒤에 정한다.
- 보호할 결정(24→6→1 복구, 추가 호출 한 단계, `extra_call_items()`·`merge_extra_call()` 두 곳만 수정).

## 결과

비어 있음. 매일 22:30 에 그날의 채택·반려와 서버 점수를 여기에 한 줄씩 적는다.
