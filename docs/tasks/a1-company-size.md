# A1 / 기업규모 세로축과 금액 결정표

상태: in_progress — 구현·로컬 검증 완료, 실제 모델·카나리 회차 대기. 새 모델 성과 없음.
담당: 사용자 지정 구현 세션, `merganser`.
원본: 사용자가 지정한 본 저장소 주 작업공간의 동명 티켓(2026-09-20 읽음).

## 하지 말 것

- 전체 법령 주입, 합동 24항목 프롬프트·스키마 수정, 금액 게이트만 추가하는 후보,
  문서 길이·검색 확대를 다시 시도하지 않는다.
- 한 회차에 다른 가설을 섞지 않는다. N1·N3는 끄고 기존 제출 기준선에 A1만 더한다.
- 공고 ID 분기·외부 자료·모델 기억으로 조문을 보완하지 않는다.
- mock·과거 원응답 재생을 새 모델 TP 회복으로 보고하지 않는다.

## 계약

- 가설: 같은 공고에서 참가자격의 기업등급을 한 번 추출하고 금액 비교를 코드로 하면
  v14~v18의 미탐과 소기업/중소기업 혼동을 줄인다.
- 입력: `open/dev.jsonl`, `open/train_unlabeled.jsonl` 앞 6,000건,
  제공 항목표·법령 스냅샷·경쟁제품 CSV. `dev_labels.csv`는 사후 채점에만 사용.
- 출력: 기업등급·적용범위·예외의 사실과 원문 인용, v14~v18 결정표 판정,
  같은 회차의 A1 적용 전 CSV·최종 CSV·진단 원응답. A2는 기업등급 사실을 재사용할 수 있다.
- 실패: 추가 호출 실패/미확인 사실은 같은 공고의 검증된 기본 판정을 보존한다.
  불완전 문서를 제한 없음으로 바꾸지 않는다. v16·v18의 e는 항상 빈칸.
- 수정 범위: `script.py`, 관련 `tests/`, `tools/replay_run.py`,
  A1 측정 도구, `reports/team-c/a1-company-size/`, 이 작업서,
  `docs/tasks.md`, `.wiki/plan-active.md`, `artifacts/review/` 및 실행 번들.
  9/20 측정 갱신: `notebooks/colab-baseline.ipynb`, `tests/test_package.py`,
  `tools/compare_runs.py`, `docs/workflow.md`, `docs/colab.md`의 진단 기본값·churn 설명.
- 규칙: 개발·로컬 검증 / A1·A4·A5·A9·A10 / R1·R3·R4·R6~R11·R15·R18~R22,
  D4·D5·W5. 외부 API·대회 제출·유료 자원 개설은 이 작업에서 수행하지 않는다.

## 통과선과 측정

| 항목 | 티켓 기준 TP/FP/FN | 통과선 |
| --- | --- | --- |
| v14 | 0/0/8 | 첫 TP |
| v15 | 0/4/6 | 첫 TP |
| v16 | 0/0/6 | 첫 TP |
| v17 | 3/37/3 | TP 3 이상, FP 10 이하 |
| v18 | 0/0/7 | 첫 TP |

위 수치는 `57761ff` 제출 회차 CSV 재집계와 일치한다. 정식 전후는 그 회차 및 새 회차의
A1 적용 전/후를 `tools/compare_runs.py`로 따로 낸다. 대상 밖 19항목도 대조한다.
서버 총시간 통과선은 6,000초(하드 한도 7,200초). 서버 기준 실측은 4,281초이며,
전건 1단계 추가 약 1,618초는 계획용 추정일 뿐 통과 증거가 아니다.

## 측정 — 2026-09-20 갱신 반영

1. 대상 항목의 TP/FP/FN과 대상 밖 19항목 변화를 본다. 과거 열한 쌍의 churn
   29~45셀 / Macro F1 영향 0.000008~0.004689는 역사적 관측이며 새 코드의 허용선이 아니다.
2. 노트북 `quality_pass`를 채택 근거로 쓰지 않는다.
3. 프롬프트·스키마 변경은 Colab에서 잰다. **Colab 회차 횟수 제한이 없어졌으므로 채택 후보는
   같은 ZIP으로 한 번 더 실행**해 그날 그 코드의 churn을 직접 잰다.
   `RUN_DIAGNOSTIC=True`가 기본이며 두 회차의 원응답을 모두 보존한다.
   ZIP·실행 코드·모델/설정·입력이 같은지 확인하고 항목별 TP/FP/FN, 바뀐 셀과 공고,
   Macro F1 차이를 기록한다. 두 회차로 얻은 한 쌍도 보편적인 상한으로 만들지 않는다.
   한 회차에 한 가설을 유지한다. 늘어난 것은 회차뿐이며 **제출 하루 1회, 서버 7,200초,
   여유 2,919초, 전건 추가 한 단계 약 1,618초, 정답 dev 200건**은 그대로다.
4. 무라벨 6,000건의 항목별 발화율/dev 발화율을 반드시 보고한다.
   금액 구간 비율·mock 비율은 발화율 대용이 아니다.
   W5 갱신(`c0a1929`): 기업등급 결정표를 dev 원응답 재생으로 고치는 후처리 후보에도
   같은 검사를 적용한다. 같은 후보 규칙으로 dev와 무라벨 보관 사실을 재평가하고
   발화 건수·비율·배율을 나란히 보고한다. 무라벨 사실 추출 결과가 없으면 미측정으로 남기며
   dev 개선만으로 채택하지 않는다. 낮은 배율·0건만으로 자동 반려하지도 않는다.

실행 등록: `python -X utf8 tools/register_run.py --inbox artifacts/inbox --code-commit <커밋>`.

## 결과

결정표 근거·실행 증거·미확인은 [A1 보고서](../../reports/team-c/a1-company-size/result.md)에 기록한다.
