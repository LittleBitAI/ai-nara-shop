# A-1 — v13 두 검증 경로의 합성 규칙

담당 C · 2026-09-21 (수치 갱신 2026-09-22) · **CPU 재생만. 모델 호출 0회 · 추가 시간 0초.**

## 0. 한 줄

company 가 **검증된 인용으로 scope 를 `general` 이라고 확정**했을 때 그것과 모순되는
앞 단계 v13 양성을 유지하지 않는다. **FP 8 → 7 · TP 3 유지 · 대상 밖 0셀.**

> **베이스가 움직여 이 보고서의 수를 다시 쟀다.** 처음 쓴 `3e3c15d` 에서는 기준이
> `4/9/2`, 후보가 `4/8/2` 였다. 현재 `main`(`c9398ad`)에서는 **기준 자체가 `3/8/3`** 이다 —
> `16` 이 TP 를, `198` 이 FP 를 잃었고 이는 이 후보와 무관한 상류 변경이다.
> **후보의 효과는 같다** — `PPS-DEV-03` 한 셀, FP −1, TP 불변.

## 1. 기준·후보·명령

| 항목 | 값 |
| --- | --- |
| 기준 commit | **`c9398ad`** (재베이스 후 재측정) |
| 기준 CSV | **HEAD 재생** `reports/runs/colab-1789902969401579900/dev-debug` · Macro 0.609274806721 |
| 후보 | `experiments/c_v13_merge_candidate.py` (`verify_company_size` 만 정의) |
| 검사 | `tests/test_c_v13_merge_candidate.py` · 10건 |
| 변경 안 한 것 | `script.py` · `tools/` · `notebooks/` — `git diff --stat` 빈 출력 |

```bash
python -X utf8 tools/replay_run.py --case reports/runs/colab-1789902969401579900/dev-debug \
  --output-dir <head>
python -X utf8 tools/replay_run.py --case reports/runs/colab-1789902969401579900/dev-debug \
  --candidate experiments/c_v13_merge_candidate.py --output-dir <cand>
python -X utf8 tools/compare_runs.py --before <head>/submission.csv --after <cand>/submission.csv \
  --truth open/dev_labels.csv --items v13 --output-dir <cmp>
```

**HEAD 재생이 이제 회차 CSV 와 다르다**(`재생 200건 · 회차 CSV와 다름`).
`3e3c15d` 에서는 동일했다. 상류가 `script.py` 를 고쳐 재생 결과가 움직였다는 뜻이고,
그래서 기준이 `4/9/2` 에서 `3/8/3` 으로 바뀌었다. **비교 기준은 여전히 HEAD 재생 CSV 이지
보관된 `submission.csv` 가 아니다** — 둘이 갈라졌으므로 이 구분이 더 중요해졌다.

## 2. 경로 분해 — 새 베이스에서 다시 쟀다

`baseline → SME → company → 후처리` 를 재생기와 같은 순서로 태운 결과다.
**`c9398ad` 기준이며, `three-stuck` 계열 보고서가 쓴 `3e3c15d` 분해와 다르다**(§2-1).

| 공고 | 정답 | base | sme | company | scope | catalog | HEAD | 경로 |
| --- | ---: | ---: | ---: | ---: | --- | --- | ---: | --- |
| 077 | 1 | 1 | 0 | **1** | competitive | True | 1 | company 되살림 |
| 078 | 1 | 1 | 0 | **1** | competitive | True | 1 | company 되살림 |
| 069 | 1 | 1 | 1 | — | competitive | None | 1 | **앞 단계 보존** |
| 056 | 0 | 1 | 1 | **1** | competitive | True | 1 | company |
| 122 | 0 | 1 | 1 | **1** | competitive | True | 1 | company |
| 184 | 0 | 1 | 0 | **1** | competitive | True | 1 | company 되살림 |
| 192 | 0 | 0 | — | **1** | competitive | True | 1 | company 신규 |
| **03** | **0** | 1 | 1 | — | **general** | None | 1 | **앞 단계 보존 · scope 모순** |
| 059 | 0 | 1 | 1 | — | competitive | None | 1 | 앞 단계 보존 |
| 148 | 0 | 1 | 1 | — | competitive | True | 1 | 앞 단계 보존 |
| 193 | 0 | 1 | 1 | — | competitive | None | 1 | 앞 단계 보존 |

company 가 1 을 쓴 것 **6건**(TP 2 · FP 4), 앞 단계 보존 **5건**(TP 1 · FP 4)이다.
합쳐 v13=1 이 11건 = **TP 3 · FP 8** 이고 §4 의 기준값과 맞는다.
FN 3건은 `074`(company 무응답) · `076`(baseline 0, sme_allowed) · **`16`** 이며
이 후보의 대상이 아니다.

### 2-1. 무엇이 움직였나 — 이 후보와 무관한 상류 변경이다

| 공고 | `3e3c15d` | `c9398ad` | 결과 |
| --- | --- | --- | --- |
| **16** | company 되살림 → 1 | company 가 **안 씀** → 0 | **TP 하나를 잃었다** |
| **198** | company 되살림 → 1 | company 가 **안 씀** → 0 | FP 하나를 잃었다 |
| **148** | company 가 씀 → 1 | company 가 **안 씀**, 앞 단계 보존 → 1 | 값은 같고 **경로만 바뀜** |

셋 다 `verify_company_size` 가 v13 을 쓰지 않게 된 것이고, **이 후보가 건드리는 자리가
아니다.** 후보는 `03` 한 셀만 닫는다. `16` 의 TP 손실은 상류에 보고할 일이지
이 후보의 효과가 아니다.

**보존 5건 중 company scope 가 v13 과 모순되는 것은 `03` 하나뿐이다.**
`03` 의 `reason` 은 `decided` 다 — scope 인용이 원문에서 확인됐고 금액 밴드까지 정해졌다.
나머지 넷은 `outside_general_scope`(competitive)이라 모순이 아니다.
`059`·`069`·`193` 은 catalog 가 `None` 이라 company 가 v13 을 못 쓴 것이고,
`148` 은 catalog 가 `True` 인데도 안 썼다 — 상류 변경이 만든 새 자리다(§2-1).

## 3. 규칙 — 새 판별축이 아니다

```
검증된 scope 가 general 이고 company 가 v13 을 쓰지 않았으면 → v13 = 0
```

근거는 **항목 정의**다. v13 의 공식 항목명은 "**중기간 경쟁제품** 소기업, 소상공인 제한"이고
(`docs/items.md`), 운영 코드 `company_size_products()` 도 같은 문장을 근거로 v13 을
`competitive` 에서만 올린다. **그 전제를 보존 경로에도 같게 적용한 것**이다.

**기각된 여섯 축으로 가르지 않는다.** 금액구간·scope·qualification·role·직생언급·인용문장이
정탐과 오탐에서 같은 값이라는 결론은 그대로다 — 이 규칙은 **경쟁제품 scope 안에서는
아무것도 가르지 않는다.** 적용 대상 밖인 건 하나를 적용 대상 밖이라고 적을 뿐이다.

**올리지 않는다.** 어떤 입력에서도 v13=1 을 새로 쓰지 않는다(검사로 고정).
`unverified_scope` 면 아무것도 하지 않는다(기본 판정 보존).

## 4. 결과

| 항목 | 전 | 후 | 바뀐 공고 |
| --- | --- | --- | --- |
| **v13** | 3 / 8 / 3 · F1 0.352941 | **3 / 7 / 3 · F1 0.375000** | `PPS-DEV-03` |

- **바뀐 셀 1 / 4800 · 대상 밖 0셀.**
- Macro F1 0.609274806721 → 0.610193924368 (**+0.000919117647**).
- TP 3건(069·077·078) 전부 유지. 검사가 건별로 고정한다.

**이 Macro 차이로 개선을 주장하지 않는다.** `compare_runs.py` 가 낸 판정 문장을 그대로 옮긴다.

> churn 과거 코드의 회차 간 실측 17~45셀, 그 Macro F1 영향 0.000008~0.010088 (13쌍)
> 이번 차이는 과거 관측 범위 안이다. 현재 코드의 churn 판정은 아니다.
> 대상 항목의 TP/FP/FN이 가설대로 움직였는지로 판단한다.
> 새 후보는 같은 ZIP을 재실행해 그날 그 코드의 churn을 직접 잰다.

**항목당 양성이 5~8건이라 한 건이 F1 을 0.1 넘게 움직인다.** FP −1 을 과대평가하지 않는다.

## 5. 발화율 — 적용 대상과 셀 변경은 다른 수다

| | dev 200건 | 무라벨 20,000건 |
| --- | ---: | --- |
| 적용 대상(검증 scope=general, company v13 미기재) | **118건 · 59.0%** | **측정 불가** |
| 실제 셀 변경 | **1건 · 0.5%** | 측정 불가 |

**적용 대상이 넓은 것 자체는 위험이 아니다.** 117건은 앞 단계도 이미 0 이라 명시적 0 을
써도 같은 값이다. **위험은 겹침에 있다** — 다른 회차에서 이 118건 중 어느 하나에 앞 단계
양성이 남으면 그것도 닫는다. dev 에서 겹친 1건은 FP 였지만 그것이 보장은 아니다.

**무라벨 배율은 이 저장소의 자료로 측정할 수 없다.** 이유를 그대로 적는다.

- `open/train_unlabeled.jsonl` 이 저장소에 없다(공개 push 때 제외된 790MB 파일).
- 이 규칙의 발화 조건은 **모델의 `company_size` 사실**(scope·scope_quote)이다. A4 의 W5 가
  잰 것은 문서·메타 같은 **입력 신호**라 원문만으로 셀 수 있었지만, 이것은 아니다.
- 무라벨 company 응답은 H2 에서 2,000건 수집됐으나 그 사실 파일이 `reports/` 에 없다.

측정하려면 무라벨 원본과 그 위의 company 응답이 함께 필요하다. **대신 입력 신호로
대체 추정하지 않았다** — 그것은 다른 것을 재는 것이다.

## 6. 시간·증거 수준

추가 모델 호출 **0회**, 추가 추론 시간 **0초**. 서버 예산(6,380/7,200초)에 영향 없다.
`verify_company_size` 안에서 이미 계산된 사실을 한 번 더 읽을 뿐이다.

**증거 수준: CPU 재생.** 저장 응답 재사용이며 **실제 GPU 회차도 대회 서버도 아니다.**
`18f07e5` 에서 dev↔서버 간격이 15% 였다. dev 에서 FP 1건 감소가 서버에서 같다고 읽지 않는다.

## 7. 채택 / 대기

| 대상 | 판단 |
| --- | --- |
| 합성 규칙 | **합격선 충족.** FP 감소 1 · TP 3 유지 · 대상 밖 0셀 |
| 채택 여부 | **대기.** 아래 두 가지를 A 가 저울질해야 한다 |

**작은 이득이다.** FP 8→7 은 항목당 양성 5~8건 위에서 F1 을 0.353→0.375 로 올린다.
한 건이 F1 을 0.1 넘게 움직이는 규모이므로 이 +1 을 과대평가하지 않는다.

**베이스가 두 번 움직이는 동안 후보의 효과는 그대로였다.** `3e3c15d` 에서 FP 9→8,
`c9398ad` 에서 FP 8→7 이고 둘 다 `PPS-DEV-03` 한 셀이다. 규칙이 베이스의 수치가 아니라
**항목 정의**에 붙어 있기 때문이다. 다만 이것은 두 지점의 관측이지 안정성의 증명은 아니다.

**대신 비용이 0 이다.** 추가 호출·시간이 없고 대상 밖 셀을 건드리지 않으며, 규칙이
새 판별축이 아니라 이미 운영 중인 항목 정의를 한 경로에 더 적용한 것이다.

## 8. 미확인 사항

- **무라벨 발화 배율.** §5 의 이유로 이 저장소에서 못 잰다.
- **겹침의 안정성.** 118건 중 앞 단계 양성이 남는 것이 회차마다 몇 건인지 모른다.
  dev 한 회차에서 1건이었다. 같은 ZIP 재실행 churn 으로 이 수가 흔들리는지 안 쟀다.
- **`03` 의 라벨 근거.** company 가 general 이라 한 것이 옳은지는 확인하지 않았다.
  이 규칙은 "company 가 general 이라 확정하면 v13 은 적용 대상이 아니다" 만 쓴다.
- **`16` 이 왜 TP 를 잃었는지.** `c9398ad` 에서 company 가 v13 을 안 쓰게 됐다(§2-1).
  **이 후보와 무관한 상류 변경이고 원인을 보지 않았다.** 상류에 알릴 일이다.
- **HEAD 재생이 보관 회차 CSV 와 갈라졌다.** `3e3c15d` 에서는 바이트 동일이었다.
  지금은 **52셀 / 43공고**가 다르고 대부분 C 항목 밖이다 —
  `v24` 25 · `e24` 8 · `v21` 4 · `v9` 4 · `v6` 3 · `v1` 2 · **`v13` 2** · 나머지 4.
  어느 쪽이 서버에 가까운지는 이 보고서가 판단하지 않는다.
  **비교 기준은 계속 HEAD 재생이고, 전·후 두 CSV 가 같은 코드에서 나오므로 이 갈라짐이
  후보의 효과 측정을 오염시키지는 않는다.**
- 실제 GPU 회차·서버 점수.
