# A1 정밀도 진단 — 0a172a2 기준

상태: 진단 및 독립 후처리 가설 재생 완료. 운영 코드·프롬프트 수정, 커밋·push, 모델 호출 없음.
기준 코드는 Git의 `0a172a2`, 원응답은 `colab-1789866561858326417/dev-debug`다.
주 작업공간의 자료를 읽었으며 이 작업트리의 브랜치를 전환하거나 병합하지 않았다.

## 실제 기준선 확인

해당 커밋의 `script.py`·`replay_run.py`로 재생한 CSV가
`reports/team-b/b3-v21-replay2/submission.csv`와 바이트 단위로 일치했다.
기존 채점기로 **Macro F1 0.48819227270314225, FP 115**를 재확인했다.
v19·v21 변경은 그대로 두었다. 아래 두 후보는 각각 이 기준선에서 출발했고 결합하지 않았다.

## 오탐의 발생 단계

| 대상 | 오탐 | A1 결정표에서 새 판정 | 사실 인용 검증 실패로 기본 판정 보존 |
| --- | ---: | ---: | ---: |
| v14 | 9 | 9 | 0 |
| v17 | 23 | 12 | 11 |

v17의 보존 11건은 `unverified_qualification` 9건, `unverified_scope` 2건이다.
따라서 두 항목을 같은 인용 필터 하나로 해결할 수 있다고 가정하지 않는다.
새 판정 오탐 중에는 기업등급 자체보다 **일반제품/경쟁제품 범위**가 다른 사례가 있다.
예를 들어 `PPS-DEV-06`·`071`·`143`·`169`·`176`은 A1의 scope가 general이지만
기존 A2의 직접생산 요구·경쟁제품 품목 대조는 True였다.

## 가설 A: 기존 A2 경쟁제품 범위를 A1에도 교차 적용

변경은 하나다. 기존 `direct_production_demand(rec)`가 요구를 찾고
`competitive_product(rec, codes) is True`이면 일반제품 대상인 v14~v18을 0으로 판정한다.
그 외에는 기존 `verify_company_size` 결과를 유지한다. A2의 함수·카탈로그·금액 특이사항
처리는 변경하지 않았다. 기업등급 정규식이나 새로운 표현 목록을 추가하지 않았다.

메모리에서 `verify_company_size`만 감싸 별도 재생한 결과를 기존 `compare_runs.compare`로
대조했다. 기록된 기준선 바이트 일치, 13셀 변경, 대상 밖 0셀, 전 항목 TP 보존을 assert로 확인했다.

| 항목 | 기준 TP/FP/FN | 범위 교차 적용 TP/FP/FN |
| --- | --- | --- |
| v14 | 6/9/2 | 6/4/2 |
| v15 | 4/0/2 | 4/0/2 |
| v16 | 1/1/5 | 1/1/5 |
| v17 | 4/23/2 | 4/15/2 |
| v18 | 0/1/7 | 0/1/7 |

전체 **0.488192272703 → 0.497463243278 (+0.009270970575)**, FP **115→102**.
변경은 v14 5셀·v17 8셀로 전부 FP 제거이며 대상 밖 판정은 0셀이다.
이 결과는 고정 원응답의 CPU 재생이라 회차 churn은 없고 새 GPU 실측은 아니다.

변경 ID: v14 `06, 071, 143, 169, 176`; v17 `086, 093, 102, 103, 139, 147, 179, 188`
(모두 `PPS-DEV-` 접두어). ID를 규칙의 조건으로 사용하지 않았다.

### 무라벨 6,000건: 같은 조건의 발화율

같은 두 함수로 제공 입력의 앞 6,000건을 읽었다. 별도 모델 호출은 없었다.

| 입력 | 조건 성립 건수 | 발화율 |
| --- | ---: | ---: |
| dev 200 | 35 | 17.5% |
| 무라벨 6,000 | 1,213 | 20.2167% |
| 무라벨/dev 발화율 배율 | — | 1.155238배 |

조건 성립 건수의 금액 구간별 분포는 dev의 1억 미만/1억~2.3억/2.3억 이상이 19/7/9,
무라벨은 771/340/100이고 금액 미확인 2건이다. 금액 미확인은 기존 A2 함수의 동작 그대로다.

**측정 범위:** 이 수치는 판정을 0으로 만드는 조건이 문서에 존재하는 빈도다.
무라벨 모델 출력이 없으므로 실제로 바뀔 양성 수·TP/FP·서버 정밀도는 측정하지 못했다.
조건이 dev 밖에 존재함은 확인했지만 1.16배를 일반화 또는 자동 채택의 보증으로 쓰지 않는다.
카탈로그 조건이나 직생 요구의 대상물 연결이 틀릴 가능성도 별도 사례 검토 대상이다.

## 가설 B: 공백·줄바꿈 인용 복원만 적용

앞선 [후속 분석](followup-analysis.md)의 공백 복원 가설을 새 기준선에 다시 적용했다.
문장부호·단어를 바꾸지 않고, 실제 관측 문서의 연속 원문 구간으로만 복원한다.
가설 A의 범위 교차 적용은 넣지 않았다.

- 전체 **0.488192272703 → 0.503065095130 (+0.014872822427)**.
- v18 **0/1/7 → 1/1/6**: `PPS-DEV-041` 회복.
- v14 7/10/1, v15 5/1/1, v16 1/2/5, v17 5/19/1.
- 전체 FP 115→114. v14·v15·v16은 각각 FP가 1건 늘어 순수 정밀도 개선은 아니다.
- 17개 판정 셀 변경, 대상 밖 판정 변화 없음.

이 후보는 모델이 쓴 인용이 있어야 발화율을 측정할 수 있다. 무라벨 사실 추출 결과가 없는
현재, 원문 문서만으로 가설 B의 발화율을 대신 계산하지 않는다. W5 검증 전 미채택이다.

## 판단

정밀도 목표에는 가설 A가 먼저 검토할 후보다. 확인된 모델 추출 범위와 A2의 품목 대조가
충돌하는 지점을 기존 함수로 연결해 dev TP 손실 없이 FP 13건을 줄였다.
v18 첫 TP 문제는 별개이며 가설 A로 해결됐다고 하지 않는다.
운영 반영은 하지 않았고, 두 후보를 결합한 최댓값을 찾아 채택하는 실험도 하지 않았다.
