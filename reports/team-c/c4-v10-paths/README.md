# C4 — v10 오탐 8건을 경로로 가른다

담당 C · 2026-09-23 · **CPU 조사만. 모델 호출 0회 · GPU 0초. 후보를 만들지 않았다.**

## 0. 결론

**경로가 안 가른다. 경로가 하나뿐이기 때문이다.**

v10 최종 양성 12건(TP 4 · FP 8)이 **전부 같은 자리**에서 나온다 —
`verify_document_requirements()` 의 `direct_production` 부재 판정이다.
`앞 단계 보존` 경로로 온 것은 **0건**이다.

**그래서 이 방향은 안 간다.** 가르는 축이 아니라 축이 없다.

## 1. 기준·명령

| 항목 | 값 |
| --- | --- |
| 기준 commit | **`d205485`** (`origin/main`) |
| 기준 CSV | **HEAD 재생** `reports/runs/colab-1789902969401579900/dev-debug` · Macro **0.618427228373** |
| 기준 v10 | **4 / 8 / 3** (계획 문서 표와 일치) |
| 변경 안 한 것 | `script.py` · `tools/` · `notebooks/` — `git diff --stat` 빈 출력 |

```bash
py -X utf8 reports/team-c/c4-v10-paths/paths.py
```

## 2. v10 을 쓸 수 있는 자리는 둘뿐이다

운영 코드를 읽어 확정했다. 추측이 아니다.

| # | 자리 | 무엇을 쓰나 |
| --- | --- | --- |
| 1 | **앞 단계 보존** | `baseline`(+ SME 재검증)이 남긴 1 을 아무도 안 덮는다 |
| 2 | **부재 판정** | `verify_document_requirements()` 가 v10 에 0 또는 1 을 쓴다 |

`company_size_products()` 는 **v12·v13 만** 쓴다 — v10 을 안 건드린다(`script.py:652`).
경쟁제품 카탈로그를 v10·v11·v12 의 적용 게이트로 주는 배선은 `script.py:91~103` 이
**이미 재고 껐다**(양성 7건 중 6건 소실). 다시 열지 않는다.

## 3. 경로별 분포 — 표가 한 줄이다

| 경로 | n | TP | FP |
| --- | ---: | ---: | ---: |
| **부재 판정** | **12** | **4** | **8** |
| 앞 단계 보존 | 0 | 0 | 0 |

12건 전부의 사실 조합까지 같다.

| 공고 | 판정 | base | sme 후 | 부재판정 | scope | `direct_production` | `requirements_complete` | reason |
| --- | --- | ---: | ---: | ---: | --- | --- | --- | --- |
| 061 | TP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| 062 | TP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| 064 | TP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| 068 | TP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| 039 | FP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| 040 | FP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| 059 | FP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| 12 | FP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| 126 | FP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| 128 | FP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| 146 | FP | 0 | 0 | 1 | competitive | None | yes | `outside_general_scope` |
| **23** | FP | **1** | **1** | 1 | competitive | None | yes | `outside_general_scope` |

**A5 분석의 관측이 현재 HEAD 에서도 성립한다.** 여덟 열 중 판정과 공고 번호를 뺀
여섯 열이 TP 4건과 FP 8건에서 **글자 하나 다르지 않다.**

`23` 만 `base=1` 인데 부재 판정도 같은 값 1 을 쓴다 — **경로를 지워도 안 바뀐다.**
즉 "앞 단계 보존" 을 손대는 어떤 규칙도 v10 에서 한 셀도 못 움직인다.

## 4. 게이트가 실제로 하는 일 — 200건 전수

경로가 왜 하나뿐인지 보려고 dev 200건을 게이트 동작으로 나눴다.

| baseline v10 | 부재 판정이 쓴 값 | 라벨 | 공고 수 |
| ---: | --- | ---: | ---: |
| 0 | `None` (적용 안 됨) | 0 | 144 |
| 0 | `None` (적용 안 됨) | **1** | **2** |
| 0 | **0** | 0 | 41 |
| 0 | **0** | **1** | **1** |
| 0 | **1** | 0 | 7 |
| 0 | **1** | **1** | 4 |
| **1** | **1** | 0 | 1 |

읽히는 것 셋.

1. **baseline 이 v10=1 을 쓴 공고는 200건 중 단 1건**(`23`)이고 그것도 FP 다.
   **v10 은 사실상 이 게이트 혼자 만든다.**
2. 게이트가 **0 을 42건에 쓴다.** 그중 1건이 라벨 1 이라 FN 하나를 만든다.
3. 게이트가 **적용조차 안 되는 146건** 중 2건이 라벨 1 이라 FN 둘을 만든다.

FN 3 = 1(게이트가 0 을 씀) + 2(게이트 적용 안 됨). 이 셋(`069`·`075`·`13`)은
`reports/team-c/a5-label-definition/five-stuck-analysis.md` 가 이미 원인을 분류했다.
**다시 분석하지 않았다.** 위 표는 FN 이 어느 통에 들어가는지만 확인한 것이다.

## 4-1. 라벨 노이즈 표와 대조 — FP 8건은 그대로다

`docs/qna.md` 의 운영진 인정 노이즈 표를 이번 12건과 맞춰 봤다.

- **FP 8건 중 노이즈 표에 있는 것은 없다.** FP 8 은 그대로 8 이다.
- 표의 **`PPS-DEV-119` v10·v11=0**(*"판정 원리상 v10·v11 로 해석될 여지"*, S7-15)은
  우리도 **0 으로 예측**하고 있어 이번 TP/FP/FN 에 안 들어간다. 다만 이 셀을 "옳게 맞힌
  음성" 으로 세지 않는다 — 라벨 쪽이 흔들리는 자리다.
- 표의 **`PPS-DEV-062` v24** 는 항목이 다르다. `062` 는 여기서 v10 TP 이고 무관하다.

## 5. 산술과 실측

전부 **실측**이다. 산술 추정을 하지 않았다. §3·§4 의 모든 수는 보관 회차의 원응답을
운영 코드에 그대로 태워 센 값이고 `paths.json` 이 소유한다.
후보를 만들지 않았으므로 **TP/FP/FN 전→후가 없고 대상 밖 바뀐 셀도 없다**(0셀).

## 6. 증거 수준

**CPU 조사(저장 응답 재사용).** 모델 호출 **0회** · GPU **0초** · 제출 CSV 미생성.
실제 GPU 회차도 대회 서버도 아니다.

## 7. 채택 / 보류 / 반려

| 대상 | 판단 |
| --- | --- |
| 경로 축으로 v10 FP 가르기 | **안 간다.** 경로가 하나뿐이라 가를 축이 없다 |
| 후보 만들기 | **안 했다.** 지시대로 분해와 판정까지다 |

**이것은 v10 이 못 고칠 항목이라는 뜻이 아니다.** "경로" 라는 축이 못 가른다는 뜻이다.
가르려면 12건이 **같은 값을 갖지 않는** 다른 사실이 필요하고, 이 티켓은 그것을 찾지 않았다.

## 8. 미확인 사항

- **12건을 가르는 다른 축.** 이 티켓은 경로만 봤다. 사실 여섯 열이 전부 같다는 것은
  "현재 소비하는 사실로는 못 가른다" 는 것이고, **소비하지 않는 사실**은 안 봤다.
- **카탈로그 대조 축.** FP 8건 중 넷(`12` 소방용헤드 · `126` 아스팔트 · `128` 농업용트랙터 ·
  `146` 자동펌프)이 메타 세부품명번호가 고시에 없다는 관측이
  `reports/team-c/b1-sentence-role/README.md` 에 있다. **그러나 이 축을 적용 게이트로 쓰는
  배선은 `script.py:91~103` 이 이미 재고 껐다**(양성 7건 중 6건 소실). 새로 열지 않았다.
- **게이트가 0 을 쓴 42건.** 그중 FN 1건 말고 나머지 41건이 옳게 막힌 것인지 안 봤다.
- **부재 판정이 적용 안 되는 146건.** `scope != competitive` 이거나 인용 미검증이다.
  안에 FN 2건이 있다. 왜 적용이 안 됐는지는 A5 가 소유한다.
