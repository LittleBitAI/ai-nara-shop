# v24는 코드 대조로 못 가린다 — 세 축을 다 재 봤다

2026-09-20. 모델을 부르지 않았다. GPU 0초. 채택한 것이 없다.
회차 `colab-1789880471715651259`(`0a8459a`)의 CSV와 dev 200건으로 잰다.

## 왜 팠나

v24가 오탐 36건으로 전 항목 중 가장 많다. F1 0.167(TP 4 · FP 36 · FN 4)이고
Macro 천장이 +0.0347이다. 오탐이 많으면 정밀도 게이트가 듣는 것이 보통이다.

## 먼저 — 모델이 근거 없이 쓴다

| v24 양성 40건의 근거가 담은 것 | FP 36건 | TP 4건 |
| --- | ---: | ---: |
| 대조할 필드가 하나도 없다 | 33 | 3 |
| 금액 | 2 | 0 |
| 지역 | 1 | 0 |
| 표제 | 0 | 1 |

근거가 아예 빈칸인 것이 FP 24건 · TP 2건이다.
빈칸 양성을 전부 내려 보면 TP 4→2 · FP 36→12 · FN 4→6, F1 0.167 → 0.182이다.
오탐이 셋으로 줄어도 재현율이 같이 죽어 이득이 거의 없다.

## 세 축을 코드가 직접 대조해 봤다

v24의 비고가 "예산, 계약방법, 지역제한, 업종"이다. 모델의 근거를 거치지 않고
**공고문과 `meta`를 코드가 직접** 맞춰 봤다.

| 축 | 불일치·양성 | 불일치·음성 | 정밀도 | 양성 8건 중 | 대조 가능 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 계약방법 (표제의 `입 찰 방 법:` vs `meta.계약방법`) | 1 | 4 | 20.0% | 1/8 | 87건 |
| 지역제한 (본문의 지역제한 문장 vs `meta.지역제한여부`) | 1 | 16 | 5.9% | 1/8 | 200건 |
| 예산 (라벨 달린 금액 vs `meta.배정예산금액`·`입찰추정가격`) | 1 | 6 | 14.3% | 1/8 | 106건 |

셋 다 양성 8건 중 1건씩만 잡고 정밀도가 20% 미만이다.
세 축이 잡는 양성도 서로 다르다(`PPS-DEV-057` · `071` · `29`) — 합쳐도 3/8이고
오탐은 최대 26건이 된다. 탐지기가 안 된다.

양성 8건 중 금액 대조가 가능했던 5건을 보면 넷이 정확히 일치한다.

```
[PPS-DEV-062] 공고 기초금액 250,000,000 · meta 250,000,000   → 일치인데 정답은 위반
[PPS-DEV-072] 공고 기초금액 42,460,000 · 추정가격 38,600,000
              meta 42,460,000 · 38,600,000                  → 일치인데 정답은 위반
```

위반이 이 세 축 밖에 있다. 남은 축은 업종이고, `meta.면허업종제한목록`과 공고문의
업종 표기를 맞추는 것은 문자열 대조로는 안 된다.

## 결론 — 이건 프롬프트 자리다

v24는 조문이 없는 대조형 항목이다(`항목표.json`: "해당 없음 — 나라장터 입력값과
공고서 내용의 일치 여부 점검"). 판정 근거가 법령이 아니라 두 자료의 차이이고,
그 차이를 코드가 못 찾으면 모델이 찾아야 한다.

그런데 지금 프롬프트가 v24에 주는 것은 한 줄뿐이다.

```
6. For v24 compare document provisions with registered metadata; quote the document, not metadata.
```

네 축(예산·계약방법·지역제한·업종)을 대조하라는 지시가 한 문장에 뭉쳐 있고, 모델은
그 결과 200건 중 40건을 위반이라 쓴다 — 실제 기저율은 8/200(4%)이다.

다음 수는 프롬프트고 그것은 GPU 회차다. 후처리로 열 자리가 없다는 것이 이 보고의 결론이다.

## 재현

이 보고의 수치는 `reports/team-c/quote-restore-adopted/submission.csv`와
`open/dev_labels.csv`·`open/dev.jsonl`·각 공고의 `meta`만으로 나온다.
측정에 쓴 세 정규식은 이 문서의 표에 조건을 적어 두었고 코드에 넣지 않았다 —
**채택한 규칙이 없으므로 `script.py`를 안 고쳤다.**

## 이 보고가 증명하지 않는 것

"v24를 못 고친다"가 아니라 "이 세 축의 코드 대조로는 못 고른다"이다.
업종 축과 프롬프트 쪽은 재지 않았다.
