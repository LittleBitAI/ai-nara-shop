# 합본 회차 `colab-1789880471715651259` — scope 절은 노린 것을 정확히 맞혔고, 그 값이 아직 아무 데도 안 쓰인다

2026-09-20. 코드 `0a8459a538cb55a02d2029b48b226f163224ce4d`, script sha256 `75e066b641e6…`.
A100-SXM4-40GB · vLLM 0.26.0 · CUDA 13.0. sample 513초 · dev 886초 · dev-debug 862초, 전부 rc=0.

## 이 회차가 답하려던 질문

`reports/team-c/competitive-gate/README.md`가 남긴 것이다 — v10·v11·v13 양성의 합집합 15건은
dev에서 경쟁제품 입찰임이 확실한데 모델의 `scope == "competitive"`가 **0건**이었다.
후보가 제대로 간 9건조차 전부 `general`이었다. 매칭기가 아니라 프롬프트라고 갈랐고,
`COMPANY_SIZE_PROMPT`의 `scope` 절 13줄을 바꾼 것이 이 회차다.

## 답 — 0건 → 15건, 아홉 건 전부 뒤집혔다

| | 이전 `0a172a2` | 이번 `0a8459a` |
| --- | ---: | ---: |
| 확실한 경쟁제품 15건 중 `scope==competitive` | **0** | **15** |
| dev 200건 전체의 `competitive` | 7 | 76 |
| `other` | 9 | 0 |
| `general` | 184 | 124 |

후보가 갔는데도 `general`이던 아홉 건(`PPS-DEV-076·077·14·16·063·060·13·074·078`)이
**전부 `competitive`**가 됐다. 진단이 지목한 자리를 정확히 맞혔다.

## 그런데 v10·v11·v12·v13은 한 셀도 안 움직였다

`facts["scope"]`는 `script.py:538` **한 자리**에서만 읽히고, 그 자리는 `verify_company_size`,
즉 v14~v18 전용이다. **v10·v11·v13에는 연결돼 있지 않다.**

15건이 열린 것은 *모델이 이제 경쟁제품을 알아본다*는 사실이고, 그 사실을 소비하는 코드가
그 세 항목에는 없다. 15건 지표를 진단으로 세운 것이 맞았고, 그것이 곧 점수는 아니었다.
**다음 수가 여기다** — 열린 `scope`를 v10·v11·v13에 잇는 것.

## 점수

| 측정 | Macro F1 |
| --- | ---: |
| 이전 회차 `0a172a2` dev-debug | 0.478105 |
| **이번 회차 dev (검증 회차)** | **0.505382** |
| **이번 회차 dev-debug** | **0.512314** |

**같은 코드·같은 회차의 두 측정이 0.006932 벌어진다.** 아래 분해는 전부 dev-debug 기준이다.

### 무엇이 얼마를 냈나 — 재생으로 분해했다 (churn 0)

| 단계 | Macro F1 | Δ |
| --- | ---: | ---: |
| 옛 원응답 + `0a172a2` 코드 | 0.478105 | — |
| **새 원응답** + `0a172a2` 코드 | 0.501097 | **+0.022992** ← scope + 회차 churn |
| 새 원응답 + 현재 코드 | 0.512314 | **+0.011218** ← v5 |

둘째 줄은 같은 코드에 원응답만 바꿔 넣은 것이라 **scope 절의 몫과 회차 churn이 섞여 있다.**
셋째 줄은 원응답이 같으므로 churn이 없다.

### 가설 A는 이 회차에서 0셀이다

`competitive_by_catalogue`는 "모델이 general이라 해도 카탈로그가 경쟁제품이면 그쪽을 믿는다"였다.
모델이 이제 스스로 `competitive`라고 말하므로 538번 줄에서 먼저 반환되고 가설 A까지 가지 않는다.

| | 가설 A 발화 |
| --- | ---: |
| 이전 회차 | 29건 |
| **이번 회차** | **0건** |

**프롬프트 수정이 가설 A를 흡수했다.** 지운다는 뜻은 아니다 — 비공개 1,853건은 dev와 다르고,
0셀이면 해가 없으므로 안전망으로 남긴다. 다만 이 회차의 +0.011218은 **전부 v5의 것**이다.

## 항목별 (이전 회차 dev-debug 대비)

**오른 것**

| 항목 | F1 | TP/FP/FN |
| --- | --- | --- |
| v14 | 0.522 → **0.750** | 6/9/2 → 6/2/2 |
| v5 | 0.556 → **0.769** | 5/6/2 → 5/1/2 |
| v16 | 0.250 → **0.400** | 1/1/5 → 2/2/4 |
| v2 | 0.444 → 0.600 | 2/0/5 → 3/0/4 |
| v17 | 0.229 → 0.320 | 4/25/2 → 4/15/2 |
| v4 | 0.800 → 0.857 | 6/3/0 → 6/2/0 |
| v6 | 0.400 → 0.429 | 3/6/3 → 3/5/3 |

**내린 것**

| 항목 | F1 | TP/FP/FN |
| --- | --- | --- |
| v15 | 0.800 → 0.727 | 4/0/2 → 4/1/2 |
| v9 | 0.455 → 0.435 | 5/11/1 → 5/12/1 |
| v24 | 0.178 → 0.167 | 4/33/4 → 4/36/4 |

v18은 여전히 **0.000**(0/2/7)이고, v10·v11·v12·v13은 변화 0셀이다.
바뀐 셀은 57/4800이며 **과거 회차 간 실측 churn 17~45셀을 넘는다.** 그 초과분이 개선인지
회차 변동인지는 같은 ZIP 재실행 전에는 못 가른다.

## `other`가 사라진 것

이전 9건의 `other`가 `competitive` 6 · `general` 3이 됐다. 그 아홉 건은 **v14~v18 정답이 하나도 없어**
`competitive`든 `other`든 결과가 같다. `general`로 간 3건만 이제 금액·자격 판정을 통과하므로
오탐 자리가 될 수 있다. v20(소프트웨어)은 `scope`를 소비하지 않으므로 무관하다.

## 아직 안 잰 것

- **같은 ZIP 재실행.** 채택 후보는 같은 날 같은 제출 ZIP을 한 번 더 돌려 그날 그 코드의 churn을
  잰다. 한 회차의 `dev`와 `dev-debug`는 이것을 대신하지 않는다 — 다만 그 둘이 0.006932 벌어진
  것은 이 코드의 변동이 작지 않다는 첫 신호다.
- **무라벨 6,000건 발화율(W5).** `scope`는 모델이 쓰는 값이라 새 무라벨 회차 없이는 못 잰다.
  `competitive`가 7/200(3.5%)에서 76/200(38%)이 됐으므로 이 확인이 특히 필요하다.
- **서버 점수.** 제출하지 않았다. dev 0.70 전에는 점수 목적의 제출을 하지 않는다.
  마지막 서버 측정은 `57761ff`(9/18) 0.2978624361이다.
