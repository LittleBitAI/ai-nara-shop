# v13 선택 재검증으로 추론 비용 축소

2026-09-17. 기준 코드 `654c556c33144cfa180239872a9ee125b73a5fde`.
사용자 승인 범위는 추론 비용 최적화이며 독립 리뷰는 기존 지시로 생략했다.
새 실제 모델 실행 및 대회 서버 성공은 미검증이다.

## 근거와 변경

입력 기록은 `colab-results-1789621345861123113.zip`이며 SHA-256은
`335796bce22ccdae6cf90e2da550a5c3575ed23feb1189b5f40e157b6c2feec8`이다.
Colab A100 40GB dev 200건에서 기본 추론 363.0초, 추가 추론 330.8초였다.
추가 v10·v11 판정은 기본과 전부 같았고, v13은 기본 양성 107건 중 101건을 음성으로 바꿨다.
따라서 비용의 주원인은 정상 공고에도 반복하는 모델 호출이다. 실패 복구 코드는 해당 실행에서
사용되지 않았으므로 제거해도 그 실행의 추론 시간은 줄지 않는다.

- 기본 24항목 메시지·16,000자 문서 상한·모델 설정은 유지한다.
- 해당 공고의 기본 v13이 1인 경우에만 v13을 재검증한다. 선택에는 정답을 사용하지 않는다.
- 추가 v10·v11 생성, 관련 지시와 직접생산확인 조문을 제거한다.
- 자격 인용은 facts에서 한 번만 생성하고 검증 후 e13으로 복사한다.
- 다른 23항목·근거 및 미선택 공고는 보존한다. 추가 호출 복구 실패도 검증된 기본 판정을 보존한다.
- Colab은 선택·생략·성공·복구 실패 건수를 대조하고 선택 수를 기본 CSV에서 독립적으로 확인한다.

## 측정

고정 모델 토크나이저와 chat template으로 기존/신규 메시지를 같은 환경에서 측정했다.
토크나이저 SHA-256: `cc8d3a0ce36466ccc1278bf987df5f71db1719b9ca6b4118264f45cb627bfe0f`.
로컬 Python 3.13.9, transformers 4.57.6, tokenizers 0.22.2.
기존 Colab transformers 5.14.1의 기록보다 각 입력이 2토큰 적으므로 아래 비교는 로컬끼리의 비교다.

| 입력 | 공고 수 | 입력 토큰 합 | 공고당 평균 | 최대 |
| --- | ---: | ---: | ---: | ---: |
| 기본 24항목, 기존/신규 동일 | 200 | 1,737,022 | 8,685.11 | 12,135 |
| 기존 추가 단계 전체 | 200 | 2,030,736 | 10,153.68 | 14,066 |
| 기존 추가 단계 중 선택 공고 | 107 | 1,040,988 | 9,728.86 | 13,890 |
| 신규 추가 단계 | 107 | 965,553 | 9,023.86 | 13,185 |

추가 호출 대상 46.5% 감소, 추가 입력 토큰 약 52.5% 감소다. 같은 선택 공고에서는
입력당 705토큰이 줄었다. 출력은 3항목 facts에서 1항목 facts로 줄였지만 실제 생성 토큰은 아직 측정하지 않았다.
신규 107건 모두 문서 축소 없이 입력 예산 14,272토큰 이내였다.
기본 200건 메시지·토큰 수·문서 예산은 이전 코드와 전부 같았다.

최신 기록의 기본 CSV에 선택된 공고의 기존 최종 v13/e13만 합치면 기존 최종 CSV와
49열 전부 일치했다. 그 기록의 F1은 0.22078771129016228이다.
이는 저장된 응답을 재사용한 선택 규칙 검사이며 **새 프롬프트의 점수 검증이 아니다**.
실제 속도 감소율 역시 입력 토큰 감소율과 같다고 보장할 수 없다.

## 실행 검사와 산출물

먼저 `test_selective_v13_preserves_sparse_rows_and_skips_empty_selection`을 작성해
기존 코드가 선택 0건에도 추가 호출해서 실패함을 확인한 뒤 수정했다.

- `python -X utf8 -m unittest tests.test_package tests.test_baseline tests.test_score`: 27개 통과.
- 선택 0건, 비연속 선택 ID, 다른 23항목 보존, 실패 시 기본 판정 보존, 기존 24→6→1 복구 검사 포함.
- 이후 비연속 인덱스의 62번 실패 위치·잘못된 인덱스 건수 검사를 추가하고 해당 검사 통과.
- `python -m ruff check script.py tests/test_baseline.py tests/test_package.py`, `git diff --check`: 통과.
- `python -X utf8 tools/package.py --output artifacts/selective-v13/submit.zip --colab-output artifacts/selective-v13/colab-bundle.zip`: 압축 해제 mock 10건·49열·기본 진입점 검사 통과.
- 노트북 실제 셀 실행을 사용하는 패키지 검사에서 선택 0건/복구 실패/건수 불일치/미선택 변경 거부 확인.

제출 후보: `artifacts/selective-v13/submit.zip`, 18,677바이트.
SHA-256: `43d40db945275844437a4ec5f07907286af792b8842e8773ad74f9f887f34182`.
script.py SHA-256: `8e9af6f00c830578a59f5f738cb5fe59c6b914c4b93ccc6faa2f115d6c2b3b55`.
기존 다운로드 ZIP·채점 중인 제출물은 변경하지 않았다.

## 다음 실제 검증

수정된 Colab 노트북은 HF_TOKEN 다운로드·동일 ZIP·서버 기본 진입점 검사를 유지한다.
실제 실행에서 선택 건수, 기본/추가 추론 시간, 24항목 점수와 원문 근거를 확인해야 한다.
기본 v13 음성의 미탐은 이번 재검증으로 회복할 수 없다. 또한 기존 중기업 포함 문구 필터의
엄격성은 이번 비용 실험에서 변경하지 않았다. 새 점수에 따라 별도로 검토할 사항이다.
다운로드 품질 기준을 낮춰 통과시키지 않았으며, 기존 오류 복구 경로도 제거하지 않았다.
Colab 통과와 비공개 1,853건 대회 서버 통과는 별개다. 최초 서버 실패 원인 확정·서버 성공은 여전히 미확인이다.
