# 3항목 품목 적용 범위·참가자격 보완

## 실제 실행 비교

입력 로그: `colab-results-1789609860516528072.zip`, 실행 커밋
`40e2cc6a95156c4b74fb174746362c4a6501baee`.
ZIP SHA-256: `3aae79e8a4ba67ad0d7fddcdc15d522e687352cc86d5eb3e0b0d0ffc7f473840`.
과거 `1c64604` 결과 ZIP의 CSV와 함께 제공 dev 정답으로 재채점했다.

| 대상 | Macro F1 | TP / FP / FN |
| --- | ---: | ---: |
| 과거 기준선 1c64604 | 0.2208013652894021 | 49 / 194 / 104 |
| 40e2cc6 기본 단계 | 0.2178182289928069 | 50 / 198 / 103 |
| 40e2cc6 별도 3항목 병합 후 | 0.21953760438037098 | 52 / 214 / 101 |

샘플 10건·dev 200건 모두 두 단계 첫 응답으로 성공했다. 실제 분할 복구는 호출되지 않았다.
같은 실행의 다른 21항목 값·근거는 동일하다. dev 기본 추론 339.4초, 별도 단계 171.2초.
v10·v11은 각각 TP 0·FP 5·FN 7/6, v13은 TP 5·FP 70·FN 1이다.
과거 기준선 미달로 다운로드 보류는 정상이며 실행 성공을 성능 개선으로 취급하지 않는다.

과거/이번 기본 예측은 20개 공고의 27개 항목 값이 다르다. dev 원본·정답·채점기 해시,
모델 리비전, pip-freeze 전체 목록은 동일하다. 기록된 GPU 종류도 A100 40GB로 같다.
기본 메시지가 같다는 로컬 대조 결과만으로 GPU 출력 동일성을 보장하지 않는다.
정확한 실행 간 변동 원인은 미확인이다.

## 관측한 오류와 가설

- `PPS-DEV-09`: 서버 품목 코드는 일치하지만 고시 특이사항은 x86을 대상으로 한다.
  규격서에는 Arm CPU가 명시되어 있다. 두 부재 항목을 모두 1로 낸 결과는 적용 조건과 맞지 않는다.
- `PPS-DEV-162`: 채혈세트 코드가 고시에 없는데 문서의 의료용 원심분리기라는 문자열로
  수처리/음식물 처리용 고시 품목이 검색됐다. 기존 조회에는 일치 출처 구분이 없었다.
- `PPS-DEV-22/158/172`: 메타의 구매 품목 코드는 제공 고시에 등재되어 있지 않다.
  기존 조회는 이 사실을 별도로 표시하지 않았다. 코드 미등재만으로 공고 전체를 비대상 확정하지는 않는다.
- `PPS-DEV-061/062/064/068`: 부재 항목 양성 사례지만 정식 품명·코드가 없어 기존 조회가 비었다.
  행사 대행·정보시스템 유지관리 등을 고시의 서비스 목록과 대조할 자료가 부족했다.
- v13 오탐 `PPS-DEV-045/052/054`의 인용에는 중소기업 또는 중·소기업도 포함된다.
  단어 등장과 참가 범위 축소를 구분해야 한다. 반면 양성 `PPS-DEV-076`에는 중소기업이라는
  문구와 소기업·소상공인 확인서 요구가 함께 있어, 중소기업 단어만으로 일괄 정상 처리해서도 안 된다.

이는 제공 문서와 예측을 대조한 원인 가설이다. 모델의 내부 판단 과정을 확인한 것은 아니다.
부재 양성 사례 일부는 일반 서류 목록·사후 제재 문구에도 직접생산 등이 등장하므로
단순 키워드 유무를 정답으로 쓰지 않는다. 누락·절단된 문서에서 실제 부재를 단정하지 않는다.

## 후보 변경

기본 24항목 호출과 3항목 별도 호출 구조를 유지하며 별도 단계의 입력만 보완했다.

- 메타 코드 → 문서 코드 → 공백을 정리한 품명 문자열 순으로 조회하고 출처를 표시한다.
  코드 일치가 이름 일치에 밀려 12행 제한 밖으로 빠지지 않도록 정렬한다. 생략 행 수도 보존한다.
- 메타 코드의 고시 미등재를 별도 표시한다. 특이사항의 원문은 보존한다.
- 용역에서 일치하는 고시 코드가 없으면 제공 CSV의 서비스 대분류 29개 품목을 보조 목록으로
  제시한다. 각 행의 코드·품명·특이사항을 유지하며 해당 목록 자체는 적용 확정이 아니다.
- 대상 품목과 용도·성능·금액·예외를 먼저 확인하고, 실제 참가자격과 일반 안내를 구분한다.
  v13은 중기업 포함 표현과 소기업·소상공인만 허용하는 필수 조건을 구분한다.

공고별로 독립 조회하며 정답·ID별 분기·다른 공고의 예측은 모델 입력에 넣지 않는다.
제공 조문·품목 파일은 변경하지 않았다. 새로운 외부 자료·의존성·모델 호출 단계도 추가하지 않았다.
dev 오답으로 개발했으므로 후속 dev 점수는 독립 검증 성능이 아니다.

## 로컬 검증

- `python -X utf8 tests/test_baseline.py`: 13개 통과, 1.727초.
  새 조회 검사에서 미구현 실패를 먼저 확인했다. 코드/문자열 구분·용도 조건 보존·서비스
  후보·정렬/생략·관측 범위·입력 보존을 검사한다. 기존 분할 복구와 21항목 보존 검사도 통과했다.
- `python -X utf8 tests/test_package.py`: 5개 통과, 20.160초.
- `python -X utf8 tests/test_score.py`: 4개 통과, 9.913초.
- 1c64604의 기본 dev 메시지와 200건 모두 문자열 동일.
- 보관된 고정 리비전 FastTokenizer 검사: 별도 단계 최소 4,550·중앙값 10,974·최대 14,261토큰.
  출력 2,048·여유 64를 더해 16,384 이내. 기존 16,000자 문서 상한에서 추가 축소 0건.
  서비스 목록은 89건에 제공된다. 실제 서버 토크나이저는 런타임에서 다시 예산을 검사한다.

로컬 모델 대역·토크나이저 검사는 실제 GPU 성공 또는 성능 향상의 증거가 아니다.
다음 검증은 새 Colab 런타임에서 실제 커밋을 확인한 뒤 샘플/dev 실행이다.
기존 품질 기준(같은 실행 기본 F1 초과 및 과거 0.2208013652894021 이상)은 유지한다.
새 후보의 실제 F1·시간·분할 복구 효과·서버 성공은 아직 미검증이다.

후보는 `artifacts/sme-applicability/submit.zip`, Colab 수동 업로드 번들은 같은 폴더의
`colab-bundle.zip`이다. 압축 해제 mock 10건·49열 검사와 모델 없는 기본 진입 실패 검사를 통과했다.
제출 ZIP SHA-256: `3983d5bf012d783b708b9860b4668eff91dc7fa390cc842883be13cb391d32eb`.
script.py SHA-256: `c9b0269970fa9c53356a5759fa2325c36df31470cfa827e880f66faa25ae52c2`.
