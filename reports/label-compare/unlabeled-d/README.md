# 무라벨 삭제 집합 D — GPU 회차

[설계 감사](../unlabeled-design-audit.md) 8절의 "다음" 1번이다. v3·v17 삭제 후보가 무라벨에서 지우는 셀을
만들기 위해 운영 `script.py` 를 무라벨 표본에 그대로 돌린다. 제출용이 아니고 서버 시간 한도와 무관하다.

## 고정한 것

| 무엇 | 값 |
| --- | --- |
| 표본 순서 | [ids.txt](ids.txt), seed 20260923, 이미 라벨을 연 40건 제외 후 19,960건을 섞은 앞 6,000개. [manifest](ids.manifest.json) |
| 이번 범위 | 앞 2,000건, 500건씩 4묶음 |
| 실행 코드 | `14f03d1d5426920cdb9ca7af55e482a3ed1c777e` — 노트북 `REPO_REF` 에 박혀 있다 |
| 입력 | `train_unlabeled.jsonl` SHA-256 `f46449fb…`, 20,000건 |
| 노트북 | [colab-unlabeled-d.ipynb](../../../notebooks/colab-unlabeled-d.ipynb) |

## Colab 에서

1. 노트북을 Colab 에서 연다(GitHub 탭 또는 파일 업로드). 런타임은 A100.
2. `MyDrive/a5/train_unlabeled.jsonl` 이 있는지 확인한다. A5 때 올린 파일이다. 없으면 PC 의
   `open/train_unlabeled.jsonl` 을 Drive 내 드라이브 → a5 폴더에 올린다.
3. 보안 비밀 `HF_TOKEN` 에 노트북 접근을 허용한다.
4. 런타임 → 모두 실행. 예상 약 2시간 30분(dev 건당 3.56초 단순 환산).
5. 끊기면 첫 셀부터 다시 모두 실행한다. Drive `MyDrive/a5/unlabeled-d-14f03d1d5426/` 의 끝난 묶음은 건너뛴다.
6. 끝나면 마지막 셀의 `unlabeled-d-results-*.zip` 을 받아 전달한다. 크면 Drive 폴더를 통째로 받는다.

한 공고가 재시도까지 실패하면 `script.py` 는 실행을 멈춘다. 노트북은 그 공고를 `failed.json`(실패 집합 F)에
적고 빼고 다시 돈다. 설계의 중단선은 F 가 1%(20건)를 넘으면 원인부터 본다는 것이다.

## 회차 1 결과 — 2,000건, D 가 모자라 6,000건으로 늘린다

[run-1790141381430477242](run-1790141381430477242/) · 2026-09-23 · A100-SXM4-40GB · 코드 `14f03d1` (`script.py` SHA-256 `cecd0ed1…` = 현재 HEAD).

- 2,000/2,000 실제 모델 성공, 네 묶음 모두 첫 시도, 실패 집합 F 0건. 합계 8,436초, 건당 3.67~3.72초
- `sme` 문서 예산 축소 0건이라 재생도 된다. 네 묶음 모두 `tools/replay_run.py` 재생이 회차 CSV 와 바이트 동일
- D 는 [tools/unlabeled_d.py](../../../tools/unlabeled_d.py) 로 CSV 에서 계산했다. [d.jsonl](run-1790141381430477242/d/d.jsonl) · [summary](run-1790141381430477242/d/summary.json)

| 항목 | B=1 | D | R | Z | D 사유 | 필요 D |
| --- | ---: | ---: | ---: | ---: | --- | ---: |
| v3 | 66 | 8 | 58 | 1,934 | 배점 구간 4 · 예산 미만 4 | 19 |
| v17 | 63 | 22 | 41 | 1,937 | 좁은 자격만 22 | 32 |

dev 대비 발화 배율(W5, dev 는 기준 회차 HEAD 재생):

| 항목 | B=1 비율 dev → 무라벨 | 배율 | D 비율 dev → 무라벨 | 배율 |
| --- | --- | ---: | --- | ---: |
| v3 | 6.0% → 3.3% | 0.55 | 2.0% → 0.40% | 0.20 |
| v17 | 5.5% → 3.15% | 0.57 | 2.0% → 1.10% | 0.55 |

v3 삭제 배율 0.20 은 A4 의 v23(0.05) 처럼 dev 에 맞춘 규칙의 경고 신호다. v3 배점 구간 D 넷은 인용이
전부 `가. 입찰 금액의 100% 이상` 으로 같은 서식(PPS-DEV-170 과 같은 납품업체 평가표)이다 — 한 서식의
반복이라 독립 관측 넷으로 세기 어렵다.

지금 크기로는 판정이 안 선다. 정탐 0건이어도 98.75% Wilson 상한이 v3 0.438(n=8, 필요 < 0.258),
v17 0.221(n=22, 필요 < 0.166)이다. 설계대로 **라벨을 열기 전에** 같은 순서로 6,000건까지 늘린다.
지금 비율로 v3 약 24, v17 약 66 이 된다. 추가 4,000건, 묶음 8개, 약 4시간 40분이다.

## D 재현

보관본은 회차별 폴더에 나뉘어 있다(u00~u03 은 회차 1, u04~u11 은 회차 2). 두 폴더를 함께 준다.

```powershell
python -X utf8 tools/unlabeled_d.py `
  --run reports/label-compare/unlabeled-d/run-1790141381430477242 `
  --run reports/label-compare/unlabeled-d/run-1790158912214587070 --out <새 폴더>
```

결과 `d.jsonl` 은 `run-1790158912214587070/d/d.jsonl` 과 바이트 동일하다. 같은 묶음이 두 폴더에 있으면 거부한다.

## 회차 2 결과 — 6,000건, v17 채택 · v3 기각

[run-1790158912214587070](run-1790158912214587070/) · u04~u11 만 담았다(u00~u03 은 회차 1 과 바이트 동일).
6,000/6,000 실제 모델 성공, 열두 묶음 모두 첫 시도, F 0건. u04~u10 재생은 회차 CSV 와 바이트 동일,
u11 은 `sme` 문서 예산 축소 1건이라 재생이 설계대로 거부된다 — D 를 CSV 에서 계산하는 이유가 이것이다.

| 항목 | B=1 | D | R | Z | D 발화 dev 대비 |
| --- | ---: | ---: | ---: | ---: | ---: |
| v3 | 181 | 14 (예산 미만 8 · 배점 구간 6) | 167 | 5,819 | 0.12 |
| v17 | 189 | 73 (좁은 자격만 73) | 116 | 5,811 | 0.61 |

v3 는 6,000건에서도 D 14 로 필요한 19 에 못 미쳐 전수, v17 은 표본 순서의 앞 32개를 라벨링했다.
라벨러는 재현율을 잰 것과 같은 Opus 5.5 · medium, 같은 프롬프트(`ba3f89c8…`)다. 46공고 실패 0, 1,606초.
[labels-opus55.jsonl](labels-opus55.jsonl). 번들에는 공고·항목표·법령만 들어가고 B·C·삭제 사유는 없다.

| 항목 | n | 손실(라벨러 1 또는 정보부족) | 95% Wilson | 98.75% 상한 | 게이트(s 하한 × F/2) | 판정 |
| --- | ---: | ---: | --- | ---: | ---: | --- |
| v3 | 14 | 0 | [0, 0.215] | 0.308 | < 0.258 | **기각** |
| v17 | 32 | 0 | [0, 0.107] | 0.163 | < 0.166 | **채택** |

판정은 7절이 미리 정한 규칙 그대로다. v3 는 지운 14셀이 전부 라벨러 기준 오탐이었지만 표본이 상한을 못 내린다.
설계는 "게이트를 통과할 때까지 n 을 늘리는 반복 검정은 하지 않는다" 고 정했으므로 여기서 기각한다.

라벨러가 전부 0 을 찍은 것이 아니다 — 46공고 24항목에서 16개 항목에 양성을 냈고, v3 셀에서는 배점표 구절
(`공공기관 등의 납품 실적 … 가. 입찰 금액의 100% 이상 나.`)을 근거로 인용한 채 0 을 냈다. v17 셀에서
v15·v18 은 0 이다 — 1억 미만 소기업 제한은 판로지원법이 예정한 모양이라는 후보의 전제와 맞는다.

위험(판정이 아니라 여기에 적는다):
- v17 상한 0.163 과 게이트 0.166 의 차이가 0.003 이다. 손실이 한 건만 나왔어도 기각이었다
- 라벨러 민감도 하한 0.566 은 dev TP 셀 5건에서 왔다. 설계 4절의 사람 확인은 아직 하지 않았다
- 게이트의 F 는 dev 기준선 F1 이다. 무라벨의 참 F 는 모른다

## 받은 뒤 (PC)

D 는 재생 없이 각 묶음의 `submission.csv` 에서 바로 계산한다. 후보는 운영 `postprocess` 가 낸 v3·v17 셀의
근거문구만 읽으므로, CSV 의 `e3`·`e17` 에 `v3_deletion`·`v17_deletion` 을 건 결과가 후보 재생과 같다.
dev 10회차에서 두 방식의 삭제 셀이 전부 같았다. 재생은 `sme` 단계 문서 예산이 축소된 공고가 있으면
거부되므로(`replay_run.replay`) 무라벨에서는 이쪽이 필요하다.

그 뒤 D 에서 v3 19건 · v17 32건을 추첨해 라벨링한다(7절 민감도 보정 D).
D 가 그보다 작으면 같은 순서로 6,000건까지 늘린다 — 노트북 `N_NOTICES` 만 바꾸고 같은 Drive 폴더로 돌리면
끝난 묶음은 건너뛴다.
