# astra 설계 감사 — 쓸 것, 고칠 것, 못 쓸 것

2026-09-23. [astra 답변](../../artifacts/review/unlabeled-design-result.md)을 검산했다.
방법론은 맞다. 다만 선행 후보 지목이 거꾸로이고, 표본 크기가 4배 과대다.
그리고 astra 가 내 보고서에서 잡은 두 숫자는 둘 다 사실이라 정정했다.

## 1. astra 가 맞은 것 — 내 보고서 두 곳을 고쳤다

| 내가 쓴 것 | 실제 | 고친 자리 |
| --- | --- | --- |
| churn 범위 `0.000008~0.015666`, 근거 `runs/reproducibility.md` | 그 파일의 범위는 `0.000008~0.010088`이다. `0.015666`은 A5 control 한 쌍의 차이이고 파일에 없다 | [227631f 결과](../submission-227631f-server-result.md) 2곳 |
| 후처리 효과 `+0.016420` | 같은 원응답끼리는 `+0.015428641306`. `+0.016420`은 `18f07e5` 재생값과 `227631f` live 회차를 맞댄 혼합차라 회차 변동이 섞였다 | 같은 문서 · [결함 가족 기록](../../.wiki/decisions/2026-09-23-092-fix-gates-that-skip-their-own-verifier.md) |

첫 번째가 더 나쁘다. `docs/tasks/a8-v20-annex-injection.md:137`이 이미
"0.015666은 관측 한 쌍의 차이이지 ±허용선이 아니다"라고 적어 뒀는데, 내가 그것을
서버 −0.00068의 허용선으로 썼다. 우리 문서가 금지한 사용을 우리가 했다.

## 2. `q < F/2` 는 근사가 아니라 정확한 임계값이다

astra 의 기준 — 양성을 지울 때 그 안의 정탐 비율 `q` 가 `F1/2` 보다 크면 F1 이 내려간다.
v13 수치로 이분 탐색하니 실측 임계 `0.176471` = `F/2` `0.176471`, 오차 `6e-16`.

기준 F1 이 낮을수록 삭제가 위험하다는 뜻이다. F1 0.2 인 항목은 삭제 85%가 옳아도 악화된다.

## 3. 후보 넷은 전부 정탐 2건에서 부호가 뒤집힌다

기준선 `colab-1789902969401579900/dev-debug` · dev 200건 · 삭제 집합은
[인용 오탐 판정](../../artifacts/review/quoted-fp-result.md)의 `오탐` 셀이다.

| 항목 | TP/FP/FN | F1 | 임계 q = F/2 | 삭제 | 뒤집히는 지점 |
| --- | --- | ---: | ---: | ---: | --- |
| v3 | 8/5/0 | 0.7619 | 0.3810 | 4 | 정탐 2건 |
| v4 | 6/3/0 | 0.8000 | 0.4000 | 3 | 정탐 2건 |
| v13 | 4/9/2 | 0.4211 | 0.2105 | 6 | 정탐 2건 |
| v17 | 5/6/1 | 0.5882 | 0.2941 | 4 | 정탐 2건 |

## 4. 표본 크기가 4배 과대다 — 일률 10%가 아니라 항목별 F/2 를 건다

astra 는 네 후보 공통으로 `q < 10%`, 98.75% Wilson 상한을 걸고 D=100 을 잡았다.
그런데 항목별 임계가 0.21~0.40 이라 10% 는 필요보다 훨씬 엄하다.
정탐 0건 관측 시 필요한 D:

| 항목 | astra `q<0.10` | 항목별 `q<F/2` |
| --- | ---: | ---: |
| v3 | 57 | 11 |
| v4 | 57 | 10 |
| v13 | 57 | 24 |
| v17 | 57 | 15 |

네 후보 합계 D 가 **400 에서 60** 으로 준다. astra 의 15~30시간(선행 1개) ·
60~120시간(넷) 확인자 예산이 여기서 크게 줄어든다.
이 한 줄이 설계를 실행 불가에서 실행 가능으로 옮긴다.

## 5. 진짜 제약은 n 이 아니라 라벨 품질이다 — 그래서 v13 선행은 거꾸로다

astra 가 `opus5-cover3.csv` 를 dev 라벨에 결합해 낸 표를 재현했다. 정확히 일치한다.
외부 라벨러(Opus 5) 자신의 재현율, dev 33건 · 95% Wilson:

| 항목 | 관측 | 구간 | 이 라벨의 `0` 을 오탐 확인에 쓸 수 있나 |
| --- | ---: | --- | --- |
| v3 | 4/4 | [0.51, 1.00] | 반증은 없다. 하한 0.51 이라 신뢰를 세운 것도 아니다 |
| v17 | 3/3 | [0.44, 1.00] | 같음 |
| v13 | 1/3 | [0.06, 0.79] | 못 쓴다 — 공개 양성 3건 중 2건을 0으로 찍었다 |
| v4 | 1/4 | [0.05, 0.70] | 못 쓴다 — 공개 양성 4건 중 3건을 0으로 찍었다 |

편향의 방향이 정확히 위험한 쪽이다. 우리는 "우리가 1로 찍고 후보가 지우는 셀이
정말 1이었나" 를 묻는데, 라벨러의 오류는 1을 0으로 찍는 것이다. 그 0 이 삭제를
옳아 보이게 만든다. q 는 구조적으로 과소평가되고, F/2 만큼 느슨한 게이트는 그대로 통과한다.

astra 는 v13 을 선행 후보로 지목했다. 그런데 v13 은 임계 q 가 가장 낮아 표본이
가장 크고(D=24), 라벨러 재현율이 가장 낮은 항목이다. 네 후보 중 최악의 출발점이다.

## 6. 그래서 무엇을 하나

1. 선행은 v17(D=15) 또는 v3(D=11)이다. v13·v4 는 Opus 라벨로 판정하지 않는다.
2. 라벨러 재현율을 먼저 측정한다. 지금 근거는 33건이고 항목당 양성 3~4건이라
   어느 항목도 구간이 닫히지 않는다. 이것을 모르고 D 를 늘리면 정밀한 편향값을 얻는다.
   — 2026-09-23 실행했다. 7절: v3·v17 만 쓸 수 있고 D 는 19·32 다.
3. `q < F/2` 는 라벨링 0건으로 지금 쓸 수 있다. 앞으로 삭제형 후보는 채택 검토 전에
   이 식부터 통과시킨다 — 기준 F1 이 낮은 항목의 삭제 규칙은 대부분 여기서 걸린다.

## 7. 라벨러 재현율 실측 — dev 양성 전부, v3·v17 만 쓸 수 있다

6절 2번을 실행했다. 2026-09-23, 사용자 지정으로 라벨러를 Opus 5.5 · effort medium 으로 바꿨다.
그래서 5절의 Opus 5 33건과 합산하지 않고, 네 항목의 dev 양성이 든 공고 19건을 전부 새로 붙였다.

- 명령: `claude -p --model claude-opus-5-5 --effort medium --allowedTools Read,Glob,Grep --disallowedTools WebSearch,WebFetch`
- 번들: 새 11건(`--ids`로 export) + 기존 cover-50 번들의 8건. prompt SHA-256 `ba3f89c8…` 는 5절 실행과 같다
- 산출: [opus55-recall11.jsonl](opus55-recall11.jsonl) · [opus55-recall8.jsonl](opus55-recall8.jsonl)과 각 manifest·CSV.
  실패 0/19, 합계 866초, 건당 평균 46초(Opus 5 는 179초)
- 판정 대상: 기준선 `colab-1789902969401579900/dev-debug` 가 1로 찍은 정답 양성 셀, 곧 우리 TP 셀.
  삭제 집합은 전부 `B=1` 이라 q 를 가리는 것은 이 셀의 민감도다. 정보부족 플래그는 네 항목에서 0건이었다

| 항목 | dev 양성 적중 | 우리 TP 셀 적중 | 95% Wilson | 이 라벨러의 `0` 을 쓸 수 있나 |
| --- | ---: | ---: | --- | --- |
| v3 | 8/8 | 8/8 | [0.676, 1.000] | 쓴다 |
| v17 | 6/6 | 5/5 | [0.566, 1.000] | 쓴다 |
| v13 | 2/6 | 2/4 | [0.150, 0.850] | 못 쓴다 |
| v4 | 1/6 | 1/6 | [0.030, 0.564] | 못 쓴다 |

5절의 판정이 모델을 바꿔도 그대로 섰다. v4·v13 은 라벨러가 양성을 0으로 찍는 쪽으로 틀린다.
v13 의 놓친 셀 하나(`PPS-DEV-077`)는 근거로 `소기업 ․ 소상공인확인서 …를 소지한 업체` 를
원문 그대로 인용하고도 0을 냈다. 후보가 지우려는 바로 그 의미 경계에서 라벨러도 틀리므로,
두 오류가 서로 독립이라는 가정이 이 항목에서 가장 약하다.

dev 가 줄 수 있는 근거는 여기서 끝났다. v3 는 양성 8건, v17 은 TP 셀 5건이 전부다.

### 민감도를 반영하면 D 는 11·15 가 아니라 19·32 다

라벨러가 참양성을 비율 `1−s` 로 놓치면 관측 `q` 는 대략 `s·q` 로 줄어든다. 그래서 관측
상한을 `s·F/2` 아래로 눌러야 참 `q < F/2` 가 선다. `s` 에 위 표의 95% 하한을 넣고
4절과 같은 98.75% Wilson 으로 정탐 0건 기준 D 를 다시 셌다.

| 항목 | 임계 F/2 | 4절 D | s 하한 | 민감도 반영 D |
| --- | ---: | ---: | ---: | ---: |
| v3 | 0.381 | 11 | 0.676 | 19 |
| v17 | 0.294 | 15 | 0.566 | 32 |

두 후보 합계 51공고, 건당 46초면 초안 약 40분이다. 이 가정은 라벨러의 놓침이 삭제 집합과
무관하다는 것이고, 7절 v13 에서 보듯 항목에 따라 깨진다. v3·v17 에서 깨지는지는 무라벨 D 를
사람이 확인해야 드러난다 — astra 설계 4절의 확인자 절차는 그대로 필요하다.

### 곁가지: 같은 8공고에서 Opus 5.5 medium 이 Opus 5 보다 덜 잡았다

24항목 전체로 Opus 5 는 TP 19 · FN 9 · FP 7, Opus 5.5 medium 은 TP 14 · FN 14 · FP 7 이다.
차이 5셀은 v2·v4·v10·v13·v14 에 한 셀씩 흩어져 있고, 반대 방향(5.5 만 잡은 셀)은 0이다. 8공고·28 양성 셀은
두 라벨러를 서열화할 표본이 아니다. 4배 빠른 대가로 재현율을 잃었을 수 있다는 경고로만 적는다.

## 실행 전 걸리는 것

astra 가 4절에서 짚은 것 — `collect()` 가 중복 ID 를 거부한다. 기존
`opus5-unlabeled40.jsonl` 은 41행 40공고로 `PPS-D-017733` 이 중복이라 그대로는 실패한다.

## 문서 언어 — 해소됐다

astra 답변은 처음에 4~6절과 3절 일부가 일본어로 나왔다. 2026-09-23 07:35 에 astra 가
한국어로 통일해 다시 썼고, 히라가나·가타카나 잔여 0을 확인했다. 위 검산은 일본어판을
읽고 한 것이지만 절 구성과 수치가 같아 결론은 그대로다.
