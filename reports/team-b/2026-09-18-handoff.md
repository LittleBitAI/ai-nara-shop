# B → A 인계 · 2026-09-18

담당 B · 브랜치 `feat/b-evidence-rules` · 기준 commit `cc1e61dd909fe514021ad5dc0a35960322470bda`.
오늘은 GPU 회차를 기다리지 않았다. **증거 수준은 전부 CPU·저장 응답 재사용이다.**
실제 GPU 회차와 대회 서버 실행은 하지 않았다.

## 넘기는 ZIP 두 개

| ZIP | 내용 | sha256 | 검사 |
| --- | --- | --- | --- |
| `artifacts/team-b/head-cc1e61d/submit.zip` | **현재 HEAD** 그대로. `script.py`·`requirements.txt`가 `git show HEAD:`와 바이트 동일 | `a5e038ea0113286877f84bf9eeef319b547f996951fa5736bcd40bb49c08b669` | package.py mock 10건·49열 PASS |
| `artifacts/team-b/b-evidence-rules/submit.zip` | HEAD + 오늘의 B1~B4 근거 대조 보정(이 브랜치의 `script.py`와 같다) | `5c3c9b69496acc6e51b6bacd6729a171ed1e665fb4f8ea0463e153cdc9da23ec` | package.py mock 10건·49열 PASS |

같은 폴더에 Colab 전용 `colab-bundle.zip`이 있다(대회 제출 금지). `artifacts/`는 git에서 제외된다.
**두 ZIP 모두 실제 모델·서버 제출은 검증하지 않았다.** mock 통과는 패키징 검사일 뿐이다.
후보 ZIP은 제출 전에 D가 Colab 회차로 정상 호출·시간·49열을 확인해야 한다.

## 결과 — 보관 원응답 재생(`colab-1789655036303880754/dev-debug`, 200건)

재생 루프: `tools/replay_run.py --verify`가 회차 CSV를 바이트 단위로 재현한다(회차 코드 `b113425`).
후보와 기준이 **같은 모델 출력**을 쓰므로 회차 간 churn이 없다. 아래 차이는 전부 후처리의 효과다.

| 티켓 | 항목 | TP | FP | FN | F1 | 규칙 |
| --- | --- | --- | --- | --- | --- | --- |
| B1 | v19 | 6→6 | 21→16 | 0→0 | 0.3636→0.4286 | 근거에 계약 시·계약체결·낙찰자 결정이 있고 `입찰`·`투찰`이 없으면 0 |
| B2 | v24 | 4→4 | 43→33 | 4→4 | 0.1455→0.1778 | 근거의 금액·지역제한·(계약방법·금액구간)이 같은 뜻의 메타 필드와 **전부 일치**하면 0 |
| B3 | v21 | 4→4 | 34→9 | 2→2 | 0.1818→0.4211 | 근거의 지분율이 모두 10% 이상이거나, 수치 없이 공동계약 불허 문구면 0 |
| B4 | v9 | 5→5 | 16→11 | 1→1 | 0.3704→0.4545 | 근거가 `…이상`으로 끝나거나, 원문에서 근거 앞뒤 300자 안에 동등·상당·이상의 제품/성능/사양이 있으면 0 |

- 담당 4항목 FP **114→69**(-45), TP 19→19. **대상 밖 변화 0셀**(바뀐 셀 45/4800).
- dev Macro F1 0.218203523963 → **0.235731350263**. 같은 모델 출력의 대조라 churn 판정 대상이 아니다.
  그래도 dev에서 규칙을 만들었으므로 이 값을 일반화 성능으로 읽지 않는다.
- **근거가 빈 양성은 하나도 내리지 않았다.** 근거가 원문에 없어 빈칸이 된 양성도 그대로다(테스트로 고정).

### 다른 회차에도 같은 규칙을 적용했다

규칙은 최종 근거문구와 그 공고만 보므로 보관 CSV 4개에 직접 적용했다. **모든 회차·모든 항목에서 TP가 줄지 않았다.**

| 회차 | v9 FP | v19 FP | v21 FP | v24 FP | 대상 밖 |
| --- | --- | --- | --- | --- | --- |
| colab-1789621345861123113 (`654c556`) | 16→11 | **20→15** | 32→10 | 43→34 | 0 |
| colab-1789650911655581500 | 16→11 | 20→15 | 31→9 | 42→33 | 0 |
| colab-1789655036303880754 dev | 16→13 | 20→15 | 34→9 | 42→35 | 0 |
| colab-1789658250172468461 | 16→12 | 20→15 | 38→11 | 44→37 | 0 |

v19의 20→15는 사용자가 사전에 확인한 수치와 같다.

### 버린 해석·손대지 않은 것

- v19를 글자 그대로 `입찰 시|투찰`로만 유지하면 TP `PPS-DEV-23`("전자입찰서 제출 마감일 전까지 보유 … 계약 시 제출")이 떨어진다.
  TP 손실이므로 버리고 `입찰|투찰`로 유지 조건을 정했다.
- v9의 "호환"은 기존 장비에 묶는 제한일 수 있어 동등품 허용 문구에서 뺐다.
- v21의 5% 지분 FP(6건)는 수치만으로 TP(5% 2건)와 구분되지 않아 그대로 뒀다. 계약법·이행방식 대조가 필요하다.
- v24에서 금액이 메타와 조금 다른 FP(`36,752,500` 대 `36,750,000` 등), 지역 목록이 메타보다 넓은 FP는 그대로 뒀다.
  공고서와 메타가 실제로 다르므로 규칙으로 내릴 근거가 없다.
- v24 근거 빈칸 26건(TP 2 포함)은 손대지 않았다.

## 변경 파일

| 파일 | 변경 |
| --- | --- |
| `script.py` | `evidence_refutes()`·`v24_consistent_with_meta()`와 정규식 상수를 추가하고, `postprocess`의 ④단계로 호출. 모델 호출·프롬프트·스키마는 바뀌지 않았다 |
| `tests/test_baseline.py` | 4항목의 내림·유지 반례와 빈 근거 보존 회귀 검사. 이식 전 코드에서는 실패한다 |
| `tools/replay_run.py` | `--script` 없이 `--verify`를 쓰면 회차 `manifest.json`의 커밋에서 `script.py`를 `git show`로 읽어 재현한다 |
| `tests/test_replay_run.py` | 재현 검사를 회차 코드로 수행. `--verify` 없는 재생은 HEAD 코드를 쓰는지 검사 |
| `experiments/b_candidate.py` | 이식 전 측정용 후보 모듈. 이식본과 결과가 바이트 동일하다. `/experiments/`는 `.gitignore` 대상이라 공유되지 않는다. 규칙 본체는 `script.py`에 있다 |

**C·D에게 알릴 것:** `replay_run.py`는 공용 도구라 B의 소유 범위 밖이다. 그래도 `script.py` 후처리를 바꾸면
HEAD 코드로는 회차를 재현할 수 없고, 그러면 팀 전원의 1단계 `--verify`가 깨진다. 그래서 고쳤다.
또 C·D가 후보를 잴 때 비교 기준은 **회차 CSV가 아니라 HEAD 재생 CSV**여야 한다
(`replay_run.py --case … --output-dir <기준>`을 후보 없이 한 번 돌린다). 회차 CSV와 비교하면 B의 규칙이 효과에 섞인다.

노트북의 "다른 23항목 보존" 검사는 영향을 받지 않는다. `baseline_submission.csv`와 `submission.csv`가
같은 `postprocess`를 거치므로 새 규칙이 두 CSV에 똑같이 적용된다(`script.py` 1060·1069행).

## 검사·실행 명령

```text
python -X utf8 tools/replay_run.py --case reports/runs/colab-1789655036303880754/dev-debug --verify
python -X utf8 tools/replay_run.py --case reports/runs/colab-1789655036303880754/dev-debug \
  --candidate experiments/b_candidate.py --output-dir reports/team-b/b4-v9-replay
python -X utf8 tools/replay_run.py --case reports/runs/colab-1789655036303880754/dev-debug \
  --output-dir reports/team-b/b5-port-replay            # 이식본. b4-v9-replay와 cmp 동일
python -X utf8 tools/compare_runs.py --items v9,v19,v21,v24 \
  --before reports/runs/colab-1789655036303880754/dev-debug/submission.csv \
  --after reports/team-b/b4-v9-replay/submission.csv --output-dir reports/team-b/b4-v9-compare
python -X utf8 tools/score.py --truth open/dev_labels.csv \
  --pred reports/team-b/b5-port-replay/submission.csv --output-dir reports/team-b/b5-port-score
python -X utf8 -m unittest tests.test_baseline tests.test_package tests.test_score \
  tests.test_register_run tests.test_diagnose_items tests.test_compare_runs tests.test_replay_run
ruff check script.py tools tests experiments ; git diff --check
python -X utf8 tools/package.py --output artifacts/team-b/<이름>/submit.zip --colab-output artifacts/team-b/<이름>/colab-bundle.zip
```

- unittest 63개 중 62개 통과. 실패 1개는 `test_package.test_venv_children_find_installed_executables`의
  `PermissionError: Operation not permitted …/venv/bin/colab-path-probe`다. **변경 전 코드에서도 같은 오류가 난다.**
  이 Mac 샌드박스가 임시 venv 실행을 막아서 생기는 환경 문제로 보이며, 원인은 확정하지 않았다. 다른 PC나 Colab에서 다시 확인해야 한다.
- ruff·`git diff --check` 통과.
- 시간: 200건 전 항목 양성을 가정한 최악의 경우에도 새 후처리는 0.075초다. 모델 호출 수는 늘지 않았다.

## 채택·다음

- 채택 제안: 네 규칙 모두 TP 손실 0, 대상 밖 회귀 0. 실제 서버 반영 여부는 A가 결정한다.
- 미확인: 비공개 1,853건에서의 일반화, 실제 GPU 회차의 정상 호출·총시간, 법률 기준의 사람 대조(v21 10% 하한, v9 동등품 허용 범위).
- 사용자 지시로 이 브랜치에 커밋하고 PR을 열었다. 병합과 대회 제출은 A가 결정한다.
- 다음 티켓: v21 5% FP를 계약법·공동이행/분담이행 메타(`공동도급구성방식`)와 대조, v24 근거 빈칸 양성에서 근거가 사라진 원인 추적.
