# 제출 후보 `9b7d743` — main HEAD 를 패키징하고 검사했다

2026-09-23. 아직 제출하지 않았다. 제출 여부와 날짜는 A 가 정한다.
이 문서는 올리기로 정했을 때 손이 멈추지 않도록 값과 검사 결과를 미리 채워 둔 것이다.

## 직전 제출 `227631f` 과 무엇이 다른가

`script.py` 를 바꾼 변경은 셋이다. 프롬프트·스키마·호출 구조·호출 수는 `227631f` 와 같다 —
세 변경 모두 모델 뒤 단계다(`git diff 227631f 9b7d743 -- script.py` 에서 프롬프트·`chat`·
`extra_call_items` 를 건드린 줄 0).

| 변경 | 무엇 | dev 재생 바뀐 셀 | 무라벨 2,000건 바뀐 셀 |
| --- | --- | ---: | ---: |
| `20b1d43` | 밴드 인용 게이트가 옆의 복원기를 부른다 | 0 | 0 (무라벨 회차 코드 `14f03d1` 에 이미 있다) |
| #96 | v3·v5·v6 적용범위 게이트 | 4 | 69 |
| #99 | v5~v7 지방 고시금액을 발주기관별 금액으로 | 0 | 0 |

`requirements.txt` 는 바뀌지 않았다.

## 올릴 파일과 대조값

로컬 패키지:
`python -X utf8 tools/package.py --output artifacts/candidate-9b7d743/submit.zip --colab-output artifacts/candidate-9b7d743/colab-bundle.zip`
→ `ZIP 검증 PASS (압축 해제 mock 10건·49열)`. mock 이므로 모델 호출·정확도 검증이 아니다.

| 대조 | 값 |
| --- | --- |
| 안의 `script.py` sha256 | `eb37c194affd1aac23b2d5e17db66a7e6d7a030fc421a75d52008983bd8d46ee` |
| 안의 `requirements.txt` sha256 | `5740d52bbd587c05f02b72dfebf0059712ea68605f9e00092e5a058faf49de0f` |
| 로컬(Windows) ZIP sha256 · 크기 | `bbb9c1002f1608a412b9f4d8d1fe29163a50678b63b1dbd3e3510db9c1b42452` · 52,514바이트 |

ZIP 자체의 sha256 은 만든 기계마다 다르므로 대조는 안의 `script.py` 로 한다.
`227631f` 의 두 파일로 로컬에서 같은 방식의 ZIP 을 만들면 `722aca77…` 이고, Colab 이 만든 그 제출 ZIP 은
`4f625d22…` 다. 안의 `script.py` 는 둘 다 `0927c7b9…` 로 같다. 올릴 ZIP 은 지금까지처럼
Colab 이 검증한 바로 그 ZIP 이고, 그 안의 `script.py` 가 위 값인지만 확인한다.

## 검사

| 검사 | 결과 | 비고 |
| --- | --- | --- |
| 패키지 mock | PASS | 루트 두 파일·UTF-8 LF·모델 없는 기본 진입 실패 확인 |
| 전체 스위트 | 447개 중 444 통과 · 1 건너뜀 · 2 오류 | 오류 둘은 환경이다 — 워크트리에 `open/train_unlabeled.jsonl` 없음(`test_a5_scope_pilot`), 위키 허브 경로 미설정(`test_setup_agents`) |
| dev 재생 | Macro 0.618427228373 | `colab-1789902969401579900/dev-debug` 원응답. `reports/team-d/d7-fp-gates` 기록값과 같다 |
| dev 재생 `227631f` 대비 | +0.009152421652 · 4셀 | v3 FP 4→2(`03`·`25`) · v5 FP 1→0(`11`) · v6 FP 1→0(`102`). 정탐 손실 0 |
| 무라벨 재생 `227631f` 대비 | 69셀 | 아래 |

### 무라벨 2,000건에서 무엇이 바뀌나

`feat/unlabeled-d-run` 의 `reports/label-compare/unlabeled-d/run-1790141381430477242`(코드 `14f03d1`,
실제 모델 응답 2,000건)를 `227631f` 와 HEAD 두 판본으로 재생했다. 모델 호출 0.

| 항목 | `227631f` 양성 | HEAD 양성 | 바뀐 셀 |
| --- | ---: | ---: | --- |
| v3 | 66 | 52 | 1→0 15 · 0→1 1 |
| v5 | 24 | 0 | 1→0 24 |
| v6 | 107 | 78 | 1→0 29 |

나머지 21항목은 0셀이다.

v5 는 무라벨에서 **양성이 전부 사라진다.** 게이트는 상한과 추정가격을 둘 다 읽고 `추정가격 < 상한` 일
때만 내리므로(`evidence_refutes` 의 v5 가지) 24건 모두 조문 구간 밖의 양성이다. 게이트가 틀린 것이
아니라 모델이 고시금액 미만 공고에 v5 를 쏜 것이다. dev 에서는 모델 양성 6건 중 1건을 내렸고(17%)
무라벨에서는 24건 중 24건이다(100%). #96 보고서의 v5 배율 1.11 은 반증이 성립하는 공고의 보유율이지
실제 모델 양성을 내린 비율이 아니다. 비공개 1,853건이 무라벨을 닮았다면 v5 예측은 거의 0 이 되고,
그때 v5 점수는 게이트가 아니라 모델이 상한 이상 공고에서 v5 를 쏘는가에 달린다.

v3 의 0→1 한 칸(`PPS-D-001922`)은 새 양성이 아니다. 모델이 1 을 냈고 옛 게이트
`performance_below_budget(rec)` 가 공고 전체를 읽어 내렸던 것을, #96 이 인용을 읽게 바꿔 되살렸다.

## 시간

호출 구조가 `227631f` 와 같으므로 그 서버 실측 6,300초(한도의 87.5%)를 그대로 기대한다.
식으로도 같은 구간이다 — `227631f` Colab 회차 추론 합 717.1초/200건 × 1,853 × 0.96~0.99 = 6,378~6,578초.
같은 코드를 서버에 두 번 올린 적이 없어 서버 회차 간 변동은 모른다.

## 아직 안 한 것

- 이 커밋의 Colab GPU 회차. `colab_pass`·`quality_pass` 와 검증된 `submit.zip` 은 그 회차가 만든다.
  노트북 첫 셀 `REPO_REF` 를 `9b7d743` 로 고정한다([Colab 실행](../docs/colab.md)).
  후처리만 바뀐 후보라 재생이 코드 효과를 정확히 준다 — `227631f` 에서 재생 0.609275 와 실제 회차
  0.610266 의 차이는 churn 범위 안이었다. 그래도 실제 회차 없이 올린 적은 없다.
- 서버 제출.

## 이 문서가 결정하지 않는 것

- 올릴지. 활성 계획의 규칙 "하루 한 번뿐인 제출에 규칙 여럿을 묶으면 무엇이 졌는지 못 가른다" 에
  이 후보도 걸린다 — v3·v5·v6 세 게이트가 한 묶음이다. 9/21 A4 묶음(v6·v9·v23)이 서버에서 졌고
  그중 v6 규칙이 이번에도 들어 있다.
- 서버 변동폭을 재려고 같은 코드를 두 번 올릴지.

## `reports/submissions.json` 에 넣을 항목

제출한 뒤 `<…>` 칸과 결과 칸을 채운다.

```json
{
  "submitted_on": "<제출일>",
  "code_commit": "9b7d743b0eb3ae24a6e5db5e9bbe32a073565fd3",
  "script_sha256": "eb37c194affd1aac23b2d5e17db66a7e6d7a030fc421a75d52008983bd8d46ee",
  "submit_zip_sha256": "<Colab validation.json 의 submit_sha256>",
  "run_id": "<Colab 회차 id>",
  "outcome": "<scored | run_error>",
  "leaderboard_macro_f1": null,
  "elapsed_seconds": null,
  "elapsed_reported": null,
  "error": null,
  "source": "사용자 보고 (대회 화면)",
  "note": "227631f 에 #96 v3·v5·v6 적용범위 게이트를 더했다(#99·20b1d43 은 dev·무라벨 0셀). dev 재생 0.609274806721 → 0.618427228373 (+0.009152, 4셀, 정탐 손실 0). 무라벨 2,000건 재생 69셀 — v5 양성 24→0."
}
```
