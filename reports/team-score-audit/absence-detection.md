# 부재탐지 3항목 — 모델은 알고 있고, 합동 프롬프트가 억누른다

2026-09-18. `colab-1789658250172468461` 회차에서 `tools/diagnose_items.py`로 v16·v18·v20을
dev 200건 전체에 별도 스키마로 물었습니다. 실제 GPU 질의이며 408초 걸렸습니다.
제출 파이프라인은 같은 회차에서 이 세 항목을 전부 0으로 냈습니다.

## 재현율이 0에서 13/18로 올라갑니다

| 항목 | 지지 | 제출 파이프라인 TP/FP/FN·F1 | 별도 질의 TP/FP/FN·F1 |
| --- | ---: | --- | --- |
| v16 | 6 | 0 / 0 / 6 · 0.000 | **6** / 139 / 0 · 0.079 |
| v18 | 7 | 0 / 0 / 7 · 0.000 | **4** / 109 / 3 · 0.067 |
| v20 | 5 | 0 / 0 / 5 · 0.000 | **3** / 109 / 2 · 0.051 |

**v16은 양성 6건을 전부 잡았습니다.** 제출 파이프라인에서 6회 연속 TP=0이던 항목입니다.

이것이 바꾸는 것은 문제의 종류입니다. "모델이 이 위반을 못 본다"가 아니라
**"모델은 보는데 24항목 합동 프롬프트·스키마가 그것을 0으로 만든다"**입니다.
그리고 남은 일은 미탐 복구가 아니라 **정밀도**입니다 — 별도 질의는 200건 중 139건을
v16 위반이라고 했습니다. 지금 F1 0.079는 파이프라인의 0.000보다 높지만 여전히 낮습니다.

## 무엇이 달랐나

같은 모델·같은 공고인데 두 질의의 차이는 셋입니다.

| | 제출 파이프라인 | 별도 진단 질의 |
| --- | --- | --- |
| 한 번에 판정하는 항목 수 | 24개 | 3개 |
| `근거문구`/인용 | 부재탐지는 스키마가 `{"type": "null"}`로 고정 | 문자열 허용, 인용을 요구 |
| 출력 형식 | 항목마다 `위반여부`·`근거문구` 두 칸 | 항목마다 요구사항·인용·판정·단계 네 칸 |

어느 것이 효과의 원인인지는 **이 한 회차로 가르지 못합니다.** 셋을 따로 떼어 보려면
변수를 하나씩 바꾼 회차가 필요합니다. C3의 첫 실험은 그것입니다.

## 이 자료의 한계 — 막힌 단계 히스토그램은 믿지 마십시오

`manifest.json`의 `stages`는 쓸 수 없습니다. 도구 설계 결함입니다.
`판정`과 `막힌_단계`를 별도 칸으로 두었더니 모델이 둘을 따로 채웠습니다 —
**`판정=1`인 370건 중 286건이 `막힌_단계 ≠ violation_found`**입니다.
쓸 수 있는 것은 `판정`과 `공고_인용`뿐입니다.
도구는 `막힌_단계` 하나로 합쳐 다시 만들었고, 다음 회차부터는 이 어긋남이 없습니다.

`공고_인용`도 절반 넘게 `null`입니다. 부재탐지 항목의 성격상 "없다"를 인용으로 보이기
어려운 것이 자연스러운 면도 있지만, 인용 없이 판정=1이 나온 건은 근거를 확인할 수 없습니다.

## C3가 여기서 이어받을 것

1. 위 세 차이 중 무엇이 재현율을 살리는지 하나씩 가른다. 항목 수를 줄이는 것만으로
   되는지, 인용 요구가 필요한지, 네 칸 형식이 필요한지.
2. 정밀도를 만든다. 지금 v16은 200건 중 139건을 위반이라고 한다. 금액 구간
   (1억 이상·고시금액 미만)과 예외 명시 여부로 거르는 것이 출발점이다 —
   [items.md](../../docs/items.md)의 v16·v18 조문을 먼저 읽는다.
3. 운영 코드 반영은 B를 거친다. 별도 호출을 늘리면 **서버 시간 여유 1,008초** 안에
   들어와야 한다 ([제출 장부](../submissions.json)). 이번 질의는 dev 200건에 408초였다.

원자료는 `reports/runs/colab-1789658250172468461/diagnose/items.jsonl`이며 한 공고 한 줄,
각 줄에 실제 정답(`truth`)이 붙어 있습니다.
