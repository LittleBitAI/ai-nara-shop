# 근거 계약과 v24 대조 축 — dev 가 0.0164 올랐고 서버는 안 갔다

2026-09-22 제출 `227631f`. 서버 0.5077356078 / 105분.
최고 기록 `18f07e5` 의 0.5084137874 대비 −0.0006781796 이다.
직전 제출 `3a30167`(A4 게이트)의 0.5057795952 대비로는 +0.0019560126 이다.
장부는 [submissions.json](submissions.json)이 소유한다. 회차 기록은
[colab-1790081509639025520](runs/colab-1790081509639025520/)이다.

## 무엇을 바꿨나

`script.py` 의 후처리 두 곳이다. 프롬프트·스키마·추론 구조·호출 수는 `18f07e5` 와 같다.

- 근거 계약 — 부재탐지·v24 밖 항목은 검증된 인용이 있어야 위반이 선다. D4-4 가 `e` 를
  원문의 연속된 부분문자열로 규정하므로 한 구절도 못 집는 위반은 그 계약을 못 채운다.
- v24 대조 축 — 조문 없는 대조형 항목이라 코드가 계약방법·지역제한·업종을 직접 대조하고,
  불일치를 하나도 못 찾으면 그 양성을 내린다. 원본 항목표의 비고가 그 축을 지정한다.

## 숫자

| | dev 재생 | 실제 dev 회차 | 서버 |
| --- | ---: | ---: | ---: |
| `18f07e5` | 0.593846165415 | — | 0.5084137874 |
| `227631f` | 0.609274806721 | 0.610265946270 | 0.5077356078 |
| 차이 | +0.015428641306 | — | −0.0006781796 |

전이율 −0.041. 다섯 쌍의 전이율은 이제 0.005 · 0.249 · 0.926 · −0.147 · −0.041 이다.
어떤 고정 계수도 맞지 않는다.

Colab 실측 0.610265946270 과 재생 예측 0.609274806721 의 차이 +0.000991 은
같은 코드의 회차 간 churn 실측 범위(0.000008~0.010088) 안이다([근거](runs/reproducibility.md)).
후처리만 바꾼 후보라 재생이 정확한 코드 효과를 주고, 실제 회차가 그것을 확인했다.

후처리 효과로 인용할 수 있는 것은 **같은 원응답 재생끼리의 +0.015428641306** 하나다.
+0.016420 은 `18f07e5` 의 재생값과 `227631f` 의 live 회차를 맞댄 혼합차라 회차 변동이
섞여 있다. 이 문서 바깥에서 +0.016420 을 후처리 효과로 인용한 자리는 정정한다.

## 내 가설이 틀렸다

제출 전에 A4 와 이번 후보를 이렇게 갈랐다.

| | A4 (서버 −0.0026) | 이번 |
| --- | --- | --- |
| 근거 출처 | dev 200건을 보고 만든 v6·v9·v23 항목별 규칙 | D4-4 제출 계약 · 제공 중기부고시 대조 |
| 적용 범위 | 항목 3개에 각각 | 19항목 균일 |
| 무라벨 배율 | v23 0.05 | A7 1.206 |
| 항목별 취사선택 | 했음 | 안 함 |

그래서 A4 를 무너뜨린 두 조건이 이번엔 없다고 적었다. **그 구분이 결과를 예측하지 못했다.**
둘 다 dev 에서 크게 올랐고 둘 다 서버에서 이득을 주지 않았다.

## 손해인지 잡음인지는 가르지 못한다

−0.0006781796 은 A4 의 −0.0026341922 보다 작다. 그리고 서버 변동폭을 한 번도 측정하지
못했다 — 같은 코드를 서버에 두 번 올린 적이 없다. dev 에서는 같은 코드가 0.000008~0.010088
흔들린다([근거](runs/reproducibility.md)). 서버는 1,853건으로 dev 의 9배라 변동이 더 작을
것이라 기대하지만 그것도 측정이 아니다.

**이 문단을 dev 폭으로 서버를 재는 데 쓰지 않는다.** dev 폭은 200건·같은 하네스의 값이고
서버는 1,853건·다른 실행이다. 한쪽의 관측 폭을 다른 쪽의 허용선으로 옮길 근거가 없다.
서버 변동을 말하려면 같은 코드를 두 번 올려야 하고, 하루 한 번뿐인 제출 한 칸을 변동
측정에 쓸지가 별도 판단이다.

어느 쪽이든 결론은 같다 — 기대한 +0.0154 가 나타나지 않았다.

## 이 회차가 남기는 규칙

1. dev 로 서버를 예측할 수 없다. 전이율이 0.005 에서 0.926 까지 흩어지고 음수가 둘이다.
   "근거의 출처가 계약·제공 자료면 전이가 다를 것" 이라는 가설도 이번에 한 번 틀렸다.
   dev 이득의 크기로 제출 여부를 정하는 방식 자체가 근거가 없다.
2. 오탐을 dev 밖에서 세는 수단이 없다. 이번 변경은 dev 오탐 35건을 걷었는데
   서버에서 그만큼의 오탐이 있었는지, 그 자리에 정탐이 섞여 있었는지 우리는 모른다.
   리더보드는 Macro 하나만 주고 항목별 지표·오답은 받을 수 없다.
3. 다음 수단은 무라벨 라벨링이다. A2 가 전면 허용하고 A3 가 외부 LLM 을 허용한다.
   우리가 1로 예측한 공고를 골라 외부 모델에 물으면 그 라벨이 예측의 정오를 직접 판정한다.
   2026-09-22 에 무라벨 40건을 라벨링했으나 표본을 항목의 텍스트 전제로 층화해
   v4·v17·v13 양성이 0건이었다 — 항목 양성률이 3% 라 그 방식으로는 안 걸린다.
   예측 기반 선택으로 바꿔야 하고 그러려면 무라벨에 회차를 한 번 돌려야 한다.

## 판단

최고 서버 점수는 0.5084137874(`18f07e5`)로 유지된다. `227631f` 의 두 규칙을 되돌리지는
않는다 — 직전 제출 대비로는 올랐고, 손해의 크기가 측정 가능한 변동폭 아래이며,
근거 계약은 제출 계약 위반을 걷어내는 것이라 점수와 무관하게 옳다.
다만 **dev 이득을 서버 이득으로 읽지 않는다.**
