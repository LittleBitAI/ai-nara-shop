# v5에 항목명의 금액 구간을 걸었다 — 그리고 다른 넷에는 걸면 안 된다

2026-09-20. 모델을 부르지 않았다. GPU 0초.
합본 회차 `colab-1789872032146524122`(`0a172a2`)의 보관 원응답으로 잰다.

## 채택 — v5

**Macro F1 0.486524 → 0.493138 (+0.006614), 바뀐 셀 4/4800, 대상 밖 0.**
항목은 **F1 0.556(5/6/2) → 0.769(5/1/2)**. 오탐 6→1, **TP 손실 0**.

v5는 「**고시금액 이상** 지역제한」이다. 추정가격이 그 미만이면 이 항목이 아니라
v6·v7(고시금액 미만)의 영역이다. **dev 양성 7건이 전부 고시금액 이상 구간**이고,
금액은 `meta.입찰추정가격`에 그대로 있으며 경계(2.3억)는 조문이 정한다.

정규식이 없다. 프롬프트도 안 건드린다. **항목명이 말하는 조건을 코드가 읽는 것뿐이다.**

### 무라벨 카나리 (W5)

| | dev 200 | 무라벨 6,000 | 배율 |
| --- | ---: | ---: | ---: |
| 고시금액 미만 (판정을 0으로 만드는 조건) | 77.50% | 86.10% | **1.111** |

금액이 `meta`에서 오므로 안정적이다. 무라벨에는 금액이 없는 공고가 82건 있고
(dev는 0건), 코드가 `price is not None`을 먼저 보므로 그 경우 게이트를 걸지 않는다.

## 반려 — 같은 방식을 다른 금액 항목에 함께 걸면 손해다

"항목명에 금액이 적혀 있으면 게이트를 건다"를 다섯에 일괄 적용해 봤다. **합계 −0.219다.**

| 항목 | 항목명이 말하는 구간 | 양성 중 구간 안 | 현재 | 게이트 후 | Δ |
| --- | --- | ---: | --- | --- | ---: |
| v2 | 고시금액 미만 실적제한 | 7/7 | 2/0/5 · 0.444 | 2/0/5 · 0.444 | +0.000 |
| v4 | 고시금액 이상 특정기관, 특정실적 | **2/6** | 6/3/0 · 0.800 | 2/1/4 · 0.444 | **−0.356** |
| **v5** | 고시금액 이상 지역제한 | **7/7** | 5/6/2 · 0.556 | 5/1/2 · **0.769** | **+0.214** |
| v6 | 고시금액 미만 지역제한 시,군,구 | 5/6 | 3/6/3 · 0.400 | 3/6/3 · 0.400 | +0.000 |
| v7 | 고시금액 미만 지역제한 인접 확대 | 6/7 | 7/0/0 · 1.000 | 6/0/1 · 0.923 | −0.077 |

**v4가 결정적이다.** 양성 6건 중 2건만 고시금액 이상 구간이다 — 항목명이 "고시금액 이상"인데
정답은 그 밖에도 있다. 항목명을 조건으로 읽는 것이 항상 맞지는 않는다.
v7도 TP를 하나 잃는다(양성 7건 중 6건만 구간 안).

**그래서 v5 하나만 건다.** 일괄 규칙으로 만들지 않는다.

## 함께 재 본 v20 — 여기는 벽이다

다음 작업 후보로 v20(SW, F1 0.000, 양성 5건, C7 미착수)을 재 봤다. **독립된 일이 아니었다.**

- 「중소 소프트웨어사업자의 사업 참여 지원에 관한 지침」 제3조 ②가 이렇게 요구한다 —
  "소프트웨어사업을 발주하는 경우 입찰공고문 또는 제안요청서에 **대기업 참여제한 하한제도
  적용 여부(적용 근거 포함)를 명시하여야 한다**." 부재탐지의 대상으로 딱 맞아 보였다.
- **그런데 "대기업 참여제한" 문구가 dev 200건 전체에 0건이다.** 양성에도 음성에도 없다.
  "없으면 위반"의 대상 문자열 자체가 없다.
- "소프트웨어" 낱말은 55건에 있고 양성은 5건이다. **낱말로는 못 가른다**(음성 50건).
- 양성 5건의 `meta.정보화사업여부`가 **전부 "미입력"**이라 등록 정보로도 못 가른다.
  금액도 1.24억~49.5억으로 흩어져 별표1의 20/40/80억 구간과 직접 대응하지 않는다.

**v10·v11과 같은 구조다** — 적용 대상("이 공고가 소프트웨어사업인가")을 코드로 못 고르고,
부재탐지라 근거도 없다. 후처리로 열 자리가 없으므로 프롬프트 쪽이다.

## 재현

```powershell
python -X utf8 tools/replay_run.py --case reports/runs/colab-1789872032146524122/dev-debug `
  --output-dir reports/team-d/d6-v5-band-replay
python -X utf8 tools/compare_runs.py --items v5 `
  --before reports/team-c/a1-scope-cross-replay/submission.csv `
  --after  reports/team-d/d6-v5-band-replay/submission.csv
```

## 이 보고가 증명하지 않는 것

**서버 점수가 아니다.** 재생은 합본 회차의 모델 출력에 후처리만 다시 돌린 것이다.
이 코드의 GPU 회차도 제출도 아직 없다.
