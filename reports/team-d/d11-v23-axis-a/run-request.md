# D11 v23 축 A — Colab 실행 안내

## 회차 2 — B-1·C 수리의 최종 확인 (지금 돌릴 것)

상태: **미실행.** 실행 코드는 **`e961f2a`** 다(`feat/d-v23-axis-a` 최신). 회차 1의 `427b971`
뒤에 무라벨 반례 수리 둘이 더해졌다 — 설명일이 제출마감보다 뒤면 대상에서 빼고(B-1),
추정가격 `0` 을 금액 미상으로 읽는다(C). **가설을 새로 넣지 않았고 프롬프트·모델·데이터는
그대로다.**

돌리는 법은 아래 §실행 과 같고 **`REPO_REF = "feat/d-v23-axis-a"`** 로 준다(브랜치 이름).
`script.py` 는 `e961f2a` 이후 안 바뀌었고 그 뒤 커밋은 문서뿐이라, 브랜치 끝을 받으면 된다.
확인할 것도 §결과에서 확인할 것 과 같으며, 여기에 하나가 더 붙는다.

| | 기대 |
| --- | --- |
| dev v23 | **5/0/0**. 회차 1과 같아야 한다 — 수리는 dev 에서 한 셀도 안 움직였다(CPU 재생 실측) |
| v23·e23 밖 | churn 을 빼면 **0셀** |
| 회차 1과의 일관성 | 같은 `dev-debug` 저울로 재면 Macro `0.688961972771`, v23 `5/0/0` 이 재현되어야 한다 |
| 실행 자체 | `colab_pass` · 문서 축소 0 · 시간 예산 변화 없음(모델 호출 수 불변) |

---

## 회차 1 — 실행 완료 (`colab-1790229556644838226`, 코드 `427b971`)

`colab_pass` · `quality_pass` · dev Macro **0.683595811155** · v23 **5/0/0**.
브랜치 코드의 CPU 재생이 회차 CSV 와 바이트 동일. 실행 기록은
[`reports/runs/colab-1790229556644838226/`](../../runs/colab-1790229556644838226/) 가 소유한다.
아래 내용은 그 회차를 준비할 때 쓴 것이며 회차 2에도 그대로 적용된다.

상태: 회차 1회를 돌려 결과 ZIP 하나를 받는다.
이 후보는 **모델 뒤 단계(`postprocess`)만 바꾼다.** 프롬프트·스키마·호출 수가 그대로여서
`docs/workflow.md` W5 상 유효한 측정은 이미 끝난 **CPU 재생**이다(아래 §측정된 것).
이 회차의 목적은 점수 판정이 아니라 **운영 환경 확인**이다.

## 이 회차가 가를 수 있는 것과 없는 것

| | |
| --- | --- |
| 가른다 | 새 `script.py` 가 GPU 환경에서 죽지 않는가(`from datetime import date` 추가·새 정규식 12개) |
| 가른다 | 새 모델 응답에서도 v23 판정이 재현되는가 — 후처리 규칙이라 원응답과 무관하게 같아야 정상 |
| 가른다 | 서버 시간 예산(호출 수 불변이므로 변화 없어야 정상) |
| **못 가른다** | **이 변경의 Macro 기여.** 증분이 `+0.004629629630`(H2) 인데 같은 코드 열한 쌍의 churn 폭이 `0.000008~0.004689` 다(W5). 회차 한 쌍의 Macro 차이는 신호가 아니다 |

그래서 결과는 Macro 가 아니라 **v23 TP/FP/FN 과 v23·e23 밖 셀 수**로 읽는다.

## 실행

1. 노트북 `notebooks/colab-baseline.ipynb` 를 Colab 에 올리고 A100 GPU 를 고른다.
2. 첫 셀의 `REPO_REF` 를 **`feat/d-v23-axis-a`** 로 바꾼다(브랜치 이름 그대로 쓴다).
   실행 코드는 그 브랜치의 `5b28715e8210256514fe39320311ee59b38a9539` 다. 40자리 SHA 를
   그대로 넣어도 된다 — 약칭 SHA 는 노트북이 clone 전에 거부한다.
3. `HF_TOKEN` 보안 비밀과 노트북 접근 권한을 켠다.
4. 위에서부터 전부 실행한다. `RUN_DIAGNOSTIC = True` 를 유지한다(원응답 보관, 약 15분 추가).
5. 마지막 셀에서 `colab-results-*.zip` 을 받는다. 중간 셀이 실패했어도 마지막 셀은 따로 실행한다.

## 결과에서 확인할 것

| | 기대 |
| --- | --- |
| `PPS-DEV-27` · `28` · `139` · `140` · `141` 의 v23 | 다섯 다 **1** (정답 1) |
| `PPS-DEV-191` 의 v23 | **0**. 다만 이 변경이 191 을 막은 것이 아니다 — 물결표 표기가 마감 앵커에 안 닿아 움직이지 않았을 뿐이다 |
| `PPS-DEV-109` 의 v23 | **0** (정답 0) |
| v23·e23 밖 셀 | 회차 churn 을 빼면 **0** 이어야 한다. `tools/compare_runs.py` 로 본다 |
| 기존 TP 손실 | 없어야 한다 |
| 실행 시간 | 직전 회차와 같아야 한다(모델 호출 수 불변) |

## 측정된 것 — CPU 재생 200건

`reports/runs/colab-1789902969401579900/dev-debug` 원응답 재생. 자세한 것은 [보고서](README.md).

| | Macro | v23 TP/FP/FN | v23·e23 밖 |
| --- | ---: | --- | ---: |
| `main` (`6663365`) | 0.662185277245 | 1/1/4 | — |
| 이 브랜치 | **0.691947182006** | **5/0/0** | **0셀** |

## 아직 없는 것

- **무라벨 발화율 미측정.** W5 가 PR 본문의 `## 무라벨 검증` 절을 요구한다. GPU 회차와 별개로
  재생으로 잴 수 있고, 머지 판단 전에 채운다.
- dev v23 F1 1.000 은 양성 표본 5건 위의 값이다. W5 대로 **성과가 아니라 과적합 경고**로 읽는다.
