---
scope: project
severity: preference
triggers: ["실행 기록", "run", "runs", "colab", "colab-1789861367622882347", "manifest", "등록"]
domain: 'run-archive'
title: "run: colab-1789861367622882347 결과 ZIP을 실행 기록에 등록"
---

# run: colab-1789861367622882347 결과 ZIP을 실행 기록에 등록

무엇. `artifacts/inbox`의 `colab-results-1789861367622882347.zip`를 `tools/register_run.py`로 등록했습니다.
결과 ZIP의 파일 66개를 `reports/runs/colab-1789861367622882347/`에 텍스트로 풀었고 ZIP 바이너리는
커밋하지 않았습니다. 두 ZIP 해시·코드 커밋·실행 환경·시간·건수·원응답 여부는 같은 폴더의
`manifest.json`에 있습니다. 제출 ZIP 해시의 출처는 `artifacts/inbox/submit.zip`입니다.

왜. 이 등록은 `d786f98` 코드가 NVIDIA A100-SXM4-40GB, vLLM 0.26.0, CUDA 13.0에서 dev-debug 200건, dev 200건, sample 10건을 완주하고 Macro F1 0.40105423676852253을 남겼다는 것만 기록하고 대회 서버 제출 성공을 증명하지 않습니다.
수치는 실행이 남긴 파일에서 그대로 옮겼고 등록 과정에서 다시 계산하지 않았습니다.
등록 검사: 결과 ZIP SHA-256 `d8ddbc3d…7f8a1`, 제출 ZIP SHA-256 `70d7b6e8…d77fd`, 파일 66개 UTF-8 해독, 최대 파일 628KB로 50MB 한도 아래,
비밀정보 패턴 0건, ZIP 경로 이탈 0건. 이 문서는 초안이며 사람이 확인한 뒤 커밋합니다.

## 이 회차가 실제로 증명한 것 — 재생 예측이 GPU에서 그대로 섰다

경쟁제품 규칙(v11·v12)이 보관 원응답 재생에서 낸 수치가 실제 모델 회차에서 똑같이 나왔다.

| 항목 | 제출본 `57761ff` | 이번 회차 | 재생이 예측한 값 |
| --- | --- | --- | --- |
| v11 | 0.000 · 0/1/6 | 0.400 · 2/2/4 | TP 2 |
| v12 | 0.000 · 0/1/6 | 0.444 · 2/1/4 | TP 2 |

TP 건수가 예측과 일치한다. 이것이 churn 밖의 신호다 — Macro F1 차이가 아니라
대상 항목의 TP/FP/FN으로 읽는다는 규약이 여기서 값을 했다.
F1=0 항목이 여덟에서 다섯으로 줄었다(v10·v14·v15·v16·v20만 남음).

Macro F1은 0.3719775476 → 0.4010542368(+0.0291)이다. 예측은 A2 +0.035에 N1 +0.0038을
더한 +0.039였고, 대상 밖 항목의 churn이 반대로 실려 그만큼 깎였다(v5 −0.111, v21 −0.126,
v24 −0.026, v6 −0.033). 이 −0.010을 코드 효과로 읽지 않는다.

N1(`SPLIT_ITEMS`)도 `568ae5d`와 같은 모양으로 재현됐다 — v18 TP 0→2에 FP 35,
v16은 TP 0 그대로에 FP 13이다.

## 시간과 게이트

`quality_pass`·`colab_pass` 둘 다 통과했고 제출 ZIP이 내려받아졌다.
회차 안 기준선 대비 `paired_macro_f1_delta`는 +0.006575로, churn 관측 상한(0.003129)보다 크다.

dev 789.0초다. 같은 쌍으로 잰 `57761ff`의 dev 683.3초 → 서버 4,281초 배율(6.265)을 적용하면
서버 약 4,943초, 한도 7,200초의 68.7%로 여유 약 2,257초다. 추정이며 보장이 아니다 —
건당 환산이 과거에 어긋난 적이 있다. 배율은 같은 코드 계열의 한 쌍에서만 얻은 값이다.

원응답을 받았다(`dev-debug`, 200건). 이 프롬프트의 후처리 후보는 이제 GPU 0초다.
지금까지 쓰던 `99ebbf1` 원응답은 제출본과 다른 프롬프트였다.

제출 ZIP은 `70d7b6e8…d77fd`이고, 안의 `script.py`는 로컬 `d786f98`과 바이트가 같다
(`ea826abd…97f345`). ZIP 해시가 로컬 패키징본과 다른 것은 Colab(Linux)과 Windows의
아카이브 메타데이터 차이이며 제출 내용물의 차이가 아니다.

출처. `docs/runs.md` · `reports/runs/colab-1789861367622882347/manifest.json` · `tools/register_run.py`
