---
scope: project
severity: preference
triggers: ["실행 기록", "run", "runs", "colab", "colab-1789725593268014232", "manifest", "등록"]
domain: 'run-archive'
title: "run: colab-1789725593268014232 결과 ZIP을 실행 기록에 등록"
---

# run: colab-1789725593268014232 결과 ZIP을 실행 기록에 등록

무엇. `artifacts/inbox`의 `colab-results-1789725593268014232.zip`를 `tools/register_run.py`로 등록했습니다.
결과 ZIP의 파일 60개를 `reports/runs/colab-1789725593268014232/`에 텍스트로 풀었고 ZIP 바이너리는
커밋하지 않았습니다. 두 ZIP 해시·코드 커밋·실행 환경·시간·건수·원응답 여부는 같은 폴더의
`manifest.json`에 있습니다. 제출 ZIP 해시의 출처는 `artifacts/inbox/submit.zip`입니다.

왜. 이 등록은 `568ae5d` 코드가 NVIDIA A100-SXM4-40GB, vLLM 0.26.0, CUDA 13.0에서 dev 200건, sample 10건을 완주하고 Macro F1 0.36127747626194834을 남겼다는 것만 기록하고 대회 서버 제출 성공을 증명하지 않습니다.
수치는 실행이 남긴 파일에서 그대로 옮겼고 등록 과정에서 다시 계산하지 않았습니다.
등록 검사: 결과 ZIP SHA-256 `5b1ea616…24f4f`, 제출 ZIP SHA-256 `f850c467…15cdd`, 파일 60개 UTF-8 해독, 최대 파일 176KB로 50MB 한도 아래,
비밀정보 패턴 0건, ZIP 경로 이탈 0건. 이 문서는 초안이며 사람이 확인한 뒤 커밋합니다.

## 판정: 반려. 따로 물으면 양성이 나오지만 정밀도가 못 쓸 수준이다

가설은 **한 번에 판정하는 항목 수(24 → 2)**였다. 합동 프롬프트는 한 글자도 안 바꾸고
부재 2항목 호출만 더했다. 게이트는 `추정가격 < 2.3억`으로 dev 155건이 통과했다.

| 항목 | TP | FP | F1 |
| --- | --- | --- | --- |
| v16 | 0→**0** | 0→**13** | 0.000000 → 0.000000 |
| v18 | 0→**2** | 0→**35** | 0.000000 → **0.090909** |

**가설은 반쯤 맞았다.** v18은 합동에서 여섯 회차 내내 한 건도 안 나오던 양성을 2건 냈다.
항목 수를 줄이면 모델이 답한다는 것 자체는 사실이다.

**그런데 못 쓴다.** v18은 TP 2에 FP 35다. 부재탐지 진단에서 본 v16 6/139와 같은 모양 —
따로 물으면 모델이 "없다"를 남발한다. Macro 기여는 v18 +0.0038뿐이고 v16은 0이다.

dev Macro F1은 **0.371978 → 0.361277**로 오히려 내려갔다. 대상 밖 손실은 우리 코드가
건드릴 수 없는 항목(v5·v21·v24 등)에서 났고 노트북의 `check_live` 가드가 같은 회차 안에서
통과했으므로 **코드 회귀가 아니라 회차 churn**이다.

시간은 785.997초로 회차 A(683.268초)보다 102.7초 길다. 추가 호출 155건의 값이다.

## 노트북에 박은 판정 문구가 느슨했다

`TP > 0 이면 채택 후보`로 적었는데 TP 2에 FP 35면 채택 후보가 아니다.
**F1로 잡았어야 했다.** 다음 실험 노트북의 판정 기준을 고친다.

## 다음에 필요한 것

이 FP를 후처리로 깎을 수 있는지가 남은 질문이고, 그것은 GPU가 아니라 보관 원응답
재생으로 잰다. **이 회차는 `RUN_DIAGNOSTIC=False`라 원응답이 없어 지금은 못 한다.**

출처. `docs/runs.md` · `reports/runs/colab-1789725593268014232/manifest.json` · `tools/register_run.py`
