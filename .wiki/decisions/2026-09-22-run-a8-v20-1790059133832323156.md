---
scope: project
severity: preference
triggers: ["실행 기록", "run", "runs", "a8", "a8-v20", "a8-v20-1790059133832323156", "1790059133832323156", "회차2", "등록", "별표 주입", "v20", "부재탐지", "기각", "churn"]
domain: 'run-archive'
title: "run: a8-v20-1790059133832323156 회차2 등록 — 두 회차 다 동률이라 A8 가설 기각"
---

# A8 v20 별표 주입 회차2 등록 — 반복 개선 없음으로 기각 확정

같은 코드 `e6d9b98`, 순서를 뒤집어 `a8 → control`, 새 A100 런타임. `live`·`complete`.
혼합 F1 a8 0.589442740036 / control 0.586932341938. v20 TP/FP/FN 은 두 군 모두 1/4/4 로
회차1 과 같고 네 군 여덟 칸이 전부 같다. v20 바뀐 공고 0건, 바뀐 셀 7/4,800 은 전부 대상 밖이다.

기각은 설계 §6 의 "동률·반복 개선 없음" 행이다. Macro 차이가 회차1 -0.002540, 회차2 +0.002510 으로
부호를 바꿨고, 같은 군을 다른 런타임에서 반복한 control 의 churn 이 -0.004076(2셀)으로 그보다 크다.
a8 churn 은 +0.000975(1셀)이고 v20 churn 은 양쪽 0 이다. OFF/ON 이 같은 조건이라
`repeat-<군>-<소비자>.json` 네 개는 군별 두 파일이 같은 값이며 독립 반복 네 번이 아니다.
`paired_changes` 는 회차2 도 4건 전부 `other` 다. `PPS-DEV-135`·`158`·`162` 는 두 회차에 공통이고
회차1 의 `PPS-DEV-28` 자리에 회차2 는 `PPS-DEV-25` 가 왔다. 검증된 전이는 두 회차 합쳐 0건이다.

대상 밖 손실도 두 회차에 공통이다 — v12 `PPS-DEV-106` FP 1→2, v17 `PPS-DEV-035` TP 5→4,
v18 `PPS-DEV-154` FP 2→3. 후보를 유지했더라도 설계 §6 의 채택 보류 조건이 걸렸다.
얻은 것은 v15 `PPS-DEV-18` TP 4→5 하나이며 대상 밖이고 기전 설명이 없다.

계약·시간·실패는 회차1 과 같이 전부 통과했다. 단계 시간 a8 265.742초 / control 247.898초,
`diagnostics.jsonl` 1,618 이벤트·호출 400회·실패 0·재시도 0·최종 미복구 0,
네 군 OFF/ON 바이트 동일, 두 회차 환경 차이는 `model_load_seconds` 뿐이다.
회차2 ZIP 이 담은 `episode-1` 35개 파일은 회차1 ZIP 과 바이트 동일이라 한 번만 셌고
이 폴더에는 `episode-2` 만 넣었다. ATTEMPT 는 결과 어디에도 없어 미기록으로 남긴다.

착수서 §6 의 남은 게이트(무라벨 6,000건 발화율·시간·독립 리뷰)는 진행하지 않는다.
유망한 후보에만 쓰는 자원이고 이 결과는 유망하지 않다. 후속 우선순위는 기존 설계 §6 을 그대로 인용한다.
근거: `reports/runs/a8-v20-1790059133832323156/`, `reports/team-c/a8-v20-annex/results.md`.
전체 GPU/서버 점수가 아니다. 운영 채택·대회 제출 없음.
