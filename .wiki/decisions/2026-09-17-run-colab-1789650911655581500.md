---
scope: project
severity: contract
triggers: ["실행 기록", "run", "runs", "colab", "colab-1789650911655581500", "36b6cc1", "재현성", "비결정성", "흔들림", "등록"]
domain: 'run-archive'
title: "run: 36b6cc1 회차를 등록하고 회차 간 흔들림 0.0025를 확인했다"
---

# run: 36b6cc1 회차를 등록하고 회차 간 흔들림 0.0025를 확인했다

무엇. `colab-results-1789650911655581500.zip`을 `tools/register_run.py`로 등록했습니다. 파일 55개를
`reports/runs/colab-1789650911655581500/`에 풀었고 ZIP 바이너리는 커밋하지 않았습니다.
`36b6cc1`의 dev Macro F1은 0.21825534501066987, 같은 회차 기본은 0.21499821620044368입니다.
품질 게이트 미달로 `submit.zip`이 내려오지 않아 제출 ZIP 해시는 실행이 적어 둔
`candidate.json`에서 가져왔습니다(`a5e038ea…8b669`). 앞서 등록한 `654c556` 회차와 셀 단위로
대조한 결과는 [reproducibility.md](../../reports/runs/reproducibility.md)가 소유합니다.

왜. 프롬프트·스키마·모델·seed·입력 토큰이 전부 같은데도 두 회차의 24항목 1차 판정이 4,800셀 중 25셀 달라져 Macro F1이 0.0025 움직였으므로, 회차가 다른 기준선과의 비교로는 그보다 작은 개선을 말할 수 없습니다.
200건 중 33건의 출력 토큰 수가 달랐고 청크 구성·공고 순서·입력 토큰 수는 전부 같았습니다.
원인은 코드·데이터가 아니라 vLLM 배치 추론의 회차 간 비결정성입니다. 한 쌍의 관측이므로
0.0025를 잡음의 크기로 일반화하지 않습니다. 추가 v13 판정의 기여는 두 회차가 정확히
같았고(+0.0032571288102262) 최종 점수 하락 -0.0025는 전부 기본 단계에서 왔습니다.

`693c695` 최적화는 의도대로 작동했습니다. 추가 호출 대상이 200건에서 107건으로 줄어 추가 추론이
330.8초에서 110.7초(-66.5%), dev 전체가 896.9초에서 694.8초(-22.5%)가 됐습니다. 서버가 2시간
한도의 86%를 쓰고 있으므로 이 절감은 점수보다 값이 큽니다. 다만 서버 시간은 dev 건당 초를
곱해 추정하면 어긋나므로 실제 제출 전에는 모릅니다. 이 회차는 아직 제출하지 않았습니다.

같이 고친 것. 두 회차의 `input_sha256`이 달랐던 것은 데이터 차이가 아니라 노트북이 `gzip.open`으로
다시 압축할 때 현재 시각을 헤더에 넣기 때문이었습니다. `mtime=0`으로 고정했습니다. 그리고
품질 게이트 미달 회차는 `submit.zip`이 안 내려오므로 등록기가 실행이 적어 둔 해시를 쓰도록
고쳤습니다. 파일이 있으면 그 해시와 대조해 다른 회차의 ZIP을 붙이는 것을 막습니다.

출처. `reports/runs/reproducibility.md` · `reports/runs/colab-1789650911655581500/manifest.json` ·
`docs/workflow.md` W5
