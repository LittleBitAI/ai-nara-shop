---
scope: project
severity: preference
triggers: []
domain: ''
title: "docs: C2 v13 후보는 세 번째 베이스에서도 FP −1 · C4 v10 은 경로가 하나뿐이라 안 갈린다"
pr: 116
merged: 2026-09-23
branch: "feat/c-v13-v10-fp"
---

# docs: C2 v13 후보는 세 번째 베이스에서도 FP −1 · C4 v10 은 경로가 하나뿐이라 안 갈린다

무엇. 회차를 안 돌렸다. 둘 다 CPU 재생·조사다 — 모델 호출 0회 · GPU 0초. C2 는 후처리 후보라 재생이 판정이고(W5, 재생 후보는 두 번째 회차가 없다), C4 는 후보를 만들지 않는 조사다.

왜. | 항목 | 값 | | --- | --- | | 기준 commit | `d205485` (`origin/main`, PR #113 머지 뒤) | | 기준 재생 Macro | 0.618427228373 | | 기준 CSV | HEAD 재생 `reports/runs/colab-1789902969401579900/dev-debug` | 계획 문서 표와 차이 없다 — v13 3/8/3 · v10 4/8/3 · Macro 도 끝자리까지 같다. 그 표의 소유자(`reports/team-d/d7-fp-gates/score/metrics.json`)는 #96 직후 재생인데, 그 뒤 들어온 #111·#112·#113 이 C 항목 재생을 한 셀도 안 건드렸다. 같은 수지만 다른 재생이라 보고서에 출처를 갈라 적었다. …

정정. 원래 제목은 **"네 번째 베이스"** 였고 그것은 틀렸다. **측정 지점은 셋(`3e3c15d` ·
`c9398ad` · `d205485`)이고 이동은 두 번이다.** 머지 뒤에 **PR #116 의 제목과 이 기록의
제목을 둘 다 고쳤다** — 이 줄은 그 사실을 남긴다. 맞는 표기의 원본은
`reports/team-c/c2-v13-remeasure/README.md` §0 의 표이고 §0-1 이 경위를 든다.
**수치는 이 오기와 무관하다** — 세 지점 전부 효과가 `03` 한 셀이고 FP −1 이다.

출처. PR #116 · `feat/c-v13-v10-fp`
