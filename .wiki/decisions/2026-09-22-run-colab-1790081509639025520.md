---
scope: project
severity: preference
triggers: ["실행 기록", "run", "runs", "colab", "colab-1790081509639025520", "manifest", "등록"]
domain: 'run-archive'
title: "run: colab-1790081509639025520 결과 ZIP을 실행 기록에 등록"
---

# run: colab-1790081509639025520 결과 ZIP을 실행 기록에 등록

무엇. `artifacts/inbox`의 `colab-results-1790081509639025520.zip`를 `tools/register_run.py`로 등록했습니다.
결과 ZIP의 파일 57개를 `reports/runs/colab-1790081509639025520/`에 텍스트로 풀었고 ZIP 바이너리는
커밋하지 않았습니다. 두 ZIP 해시·코드 커밋·실행 환경·시간·건수·원응답 여부는 같은 폴더의
`manifest.json`에 있습니다. 제출 ZIP 해시의 출처는 `artifacts/inbox/submit.zip`입니다.

왜. 이 회차가 오늘 채택한 두 규칙 — 근거 계약(PR #91 커밋 1)과 v24 대조 축(커밋 2) — 을
실제 모델 추론 위에서 처음 확인했습니다. 두 규칙은 모두 보관 원응답 재생으로만 측정했고
재생 예측은 `0.609274806721`이었습니다. 이번 실측 `0.610265946270`과의 차이 `+0.000991`은
같은 코드의 회차 간 churn 실측 범위(`0.000008`~`0.015666`) 안입니다. 직전 최고 서버 제출
`18f07e5`의 같은 dev 재생이 `0.593846165415`이므로 dev 기준 `+0.016420`입니다.

회차 중 Colab 게이트가 한 번 걸렸고 그것이 이 회차를 돌린 이유입니다. `sme_selection`은
후처리 **전** 원판정의 v13 양성으로 고르는데 `baseline_submission.csv`는 후처리 뒤라,
근거 계약이 검증된 인용 없는 양성을 내리면 두 수가 갈립니다(dev 200건에서 104건 중 25건).
게이트가 상등을 요구해 터졌고 `227631f`가 포함 관계 검사로 고쳤습니다. `script.py`는
바뀌지 않았고 서버는 이 단언을 돌리지 않습니다. 이번 회차의 v13 선택은 107건, 폴백 0건입니다.

이 등록은 `227631f` 코드가 NVIDIA A100-SXM4-40GB, vLLM 0.26.0, CUDA 13.0에서 dev 200건, sample 10건을 완주하고 Macro F1 0.6102659462701191을 남겼다는 것만 기록하고 대회 서버 제출 성공을 증명하지 않습니다.
수치는 실행이 남긴 파일에서 그대로 옮겼고 등록 과정에서 다시 계산하지 않았습니다.
등록 검사: 결과 ZIP SHA-256 `374cf44a…b69ea`, 제출 ZIP SHA-256 `4f625d22…549aa`, 파일 57개 UTF-8 해독, 최대 파일 251KB로 50MB 한도 아래,
비밀정보 패턴 0건, ZIP 경로 이탈 0건. 이 문서는 초안이며 사람이 확인한 뒤 커밋합니다.

출처. `docs/runs.md` · `reports/runs/colab-1790081509639025520/manifest.json` · `tools/register_run.py`
