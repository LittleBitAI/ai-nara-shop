---
scope: project
severity: contract
triggers: ["실행 기록", "run", "runs", "colab", "colab-1789655036303880754", "manifest", "등록"]
domain: 'run-archive'
title: "run: colab-1789655036303880754 결과 ZIP을 실행 기록에 등록"
---

# run: colab-1789655036303880754 결과 ZIP을 실행 기록에 등록

무엇. `artifacts/inbox`의 `colab-results-1789655036303880754.zip`를 `tools/register_run.py`로 등록했습니다.
결과 ZIP의 파일 64개를 `reports/runs/colab-1789655036303880754/`에 텍스트로 풀었고 ZIP 바이너리는
커밋하지 않았습니다. 두 ZIP 해시·코드 커밋·실행 환경·시간·건수·원응답 여부는 같은 폴더의
`manifest.json`에 있습니다. 제출 ZIP 해시의 출처는 `candidate.json`입니다.

왜. 이 회차가 같은 `script.py`의 세 번째·네 번째 측정을 주어, 앞서 "회차 간 Macro F1이 0.0025 흔들린다"고 적은 결론이 한 번의 뽑기였음을 드러냈습니다.
같은 코드 세 쌍에서 셀은 33~41개 바뀌었는데 Macro F1은 0.000008~0.000044만 움직였습니다.
`654c556`과의 0.0025도 코드 효과가 아닙니다 — run1과의 두 차이가 26%만 겹치고 방향도 7:6으로
균형입니다. 즉 churn의 점수 영향이 0.000008~0.002576으로 300배 벌어지며 셀 수로는 구분되지
않습니다. 그래서 한 쌍의 Macro F1 차이는 이 규모에서 신호가 아니고, 쓸 수 있는 것은 대상
항목의 TP/FP/FN뿐입니다. [reproducibility.md](../../reports/runs/reproducibility.md)를 그 결론으로
다시 썼고 `docs/workflow.md` W5·`tools/compare_runs.py`·인수인계 §0을 함께 고쳤습니다.

`b113425`의 dev Macro F1은 0.21821144133644133, `dev-debug`는 0.2182035240입니다. 등록 검사는
결과 ZIP SHA-256 `f78effb5…f5011`, 제출 ZIP `a5e038ea…8b669`(품질 게이트 미달로 파일이 안 와
`candidate.json`에서), 파일 64개 UTF-8 해독, 최대 554KB, 비밀정보 0건, 경로 이탈 0건입니다.

원응답 307건을 처음 보관했습니다. `postprocess`·`verify_sme`는 입력이 모델 응답·공고 원문·
고시 CSV뿐이므로 후처리·재검증 후보를 이제 GPU 없이 잽니다. 같은 회차에서 `diagnose_items.py`는
실패했습니다 — Colab 번들에 그 파일이 없었고 `script.py`도 `submission/`에 있었습니다.
번들 목록과 `--script` 인자를 고치고, 스텁이 아니라 번들 배치에서 실제로 실행하는 검사를 넣었습니다.

출처. `reports/runs/reproducibility.md` · `reports/runs/colab-1789655036303880754/manifest.json` ·
`docs/workflow.md` W5
