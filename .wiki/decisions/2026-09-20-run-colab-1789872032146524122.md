---
scope: project
severity: preference
triggers: ["실행 기록", "run", "runs", "colab", "colab-1789872032146524122", "manifest", "등록"]
domain: 'run-archive'
title: "run: colab-1789872032146524122 결과 ZIP을 실행 기록에 등록"
---

# run: colab-1789872032146524122 결과 ZIP을 실행 기록에 등록

무엇. `artifacts/inbox`의 `colab-results-1789872032146524122.zip`를 `tools/register_run.py`로 등록했습니다.
결과 ZIP의 파일 69개를 `reports/runs/colab-1789872032146524122/`에 텍스트로 풀었고 ZIP 바이너리는
커밋하지 않았습니다. 두 ZIP 해시·코드 커밋·실행 환경·시간·건수·원응답 여부는 같은 폴더의
`manifest.json`에 있습니다. 제출 ZIP 해시의 출처는 `artifacts/inbox/submit.zip`입니다.

왜. 이 등록은 `0a172a2` 코드가 NVIDIA A100-SXM4-40GB, vLLM 0.26.0, CUDA 13.0에서 dev-debug 200건, dev 200건, sample 10건을 완주하고 Macro F1 0.4780515523487083을 남겼다는 것만 기록하고 대회 서버 제출 성공을 증명하지 않습니다.
수치는 실행이 남긴 파일에서 그대로 옮겼고 등록 과정에서 다시 계산하지 않았습니다.
등록 검사: 결과 ZIP SHA-256 `fe97c5ec…8e9b9`, 제출 ZIP SHA-256 `01fca075…e8823`, 파일 69개 UTF-8 해독, 최대 파일 817KB로 50MB 한도 아래,
비밀정보 패턴 0건, ZIP 경로 이탈 0건. 이 문서는 초안이며 사람이 확인한 뒤 커밋합니다.

## 이 회차가 증명한 것과 뒤집은 것

**A1+A2+v19+v21 합본의 첫 실제 GPU 실행이다.** dev **0.4780515523** —
제출본 `57761ff`(서버 0.2978624361, dev 0.3719775476) 대비 **+0.1061**이다.
`colab_pass`·`quality_pass` 통과. **F1=0 항목이 여덟에서 셋으로 줄었다**(v10·v18·v20).
TP 73→86, FP 140→129. 서버 시간 추정 5,451초(75.7%)로 한도 안이다.

보관 원응답이 회차 CSV를 **바이트 단위로 재현**한다(`--verify` 통과).
회차 안 `dev`↔`dev-debug`는 0.0000532로 거의 붙어 있다.

### 그런데 재생 예측 0.488192가 실측 0.478105로 −0.0101 어긋났다

**이것이 이 회차가 산 가장 값비싼 숫자다.** 예측은 A1 회차(`colab-1789866561858326417`)의
원응답에 같은 통합 후처리를 얹은 값이었다. 두 회차는 **프롬프트·스키마가 같다** —
A2·v19·v21은 전부 모델 뒤 단계라 모델에게 가는 것이 한 글자도 다르지 않다.
**그러므로 −0.0101은 후처리의 오차가 아니라 전부 모델 출력의 흔들림이다.**

바뀐 셀은 **17개**다. 기존 churn 관측의 셀 하한(29)보다 **적은데** Macro F1 영향은
기존 상한(0.004689)의 **2.2배**다. 이유는 지지 건수다 — `PPS-DEV-02` 한 건이 v2의 TP를
3에서 2로 떨어뜨렸고, 양성이 7건인 항목이라 F1이 0.600에서 0.444로 옮겨
24항목 평균에서 그 한 항목만 −0.0065다.

**churn 상한을 0.004689에서 0.010088로, 셀 하한을 29에서 17로 갱신했다**
([표](../../reports/runs/reproducibility.md), `tools/compare_runs.py`의 `DRIFT_*`).
**이제 한 쌍의 회차 비교로는 0.011 미만의 어떤 개선도 주장할 수 없다.**

**다만 재생 비교에는 이 값을 적용하지 않는다.** 같은 원응답을 쓰면 churn이 0이고
후보와 기준의 차이가 곧 효과다. v19(+0.0112)·v21(+0.0071)은 재생으로 잰 값이므로
위 0.010088과 견주어 "churn 범위 안"이라고 읽으면 틀린다.

출처. `docs/runs.md` · `reports/runs/colab-1789872032146524122/manifest.json` · `tools/register_run.py`
