---
scope: project
severity: preference
triggers: ["실행 기록", "run", "runs", "colab", "colab-1789716500947261692", "manifest", "등록"]
domain: 'run-archive'
title: "run: colab-1789716500947261692 결과 ZIP을 실행 기록에 등록"
---

# run: colab-1789716500947261692 결과 ZIP을 실행 기록에 등록

무엇. `artifacts/inbox`의 `colab-results-1789716500947261692.zip`를 `tools/register_run.py`로 등록했습니다.
결과 ZIP의 파일 55개를 `reports/runs/colab-1789716500947261692/`에 텍스트로 풀었고 ZIP 바이너리는
커밋하지 않았습니다. 두 ZIP 해시·코드 커밋·실행 환경·시간·건수·원응답 여부는 같은 폴더의
`manifest.json`에 있습니다. 제출 ZIP 해시의 출처는 `artifacts/inbox/submit.zip`입니다.

왜. 이 등록은 `57761ff` 코드가 NVIDIA A100-SXM4-40GB, vLLM 0.26.0, CUDA 13.0에서 dev 200건, sample 10건을 완주하고 Macro F1 0.3719775475710831을 남겼다는 것만 기록하고 대회 서버 제출 성공을 증명하지 않습니다.
수치는 실행이 남긴 파일에서 그대로 옮겼고 등록 과정에서 다시 계산하지 않았습니다.
등록 검사: 결과 ZIP SHA-256 `717260a5…96dfa`, 제출 ZIP SHA-256 `25180240…c5f70`, 파일 55개 UTF-8 해독, 최대 파일 126KB로 50MB 한도 아래,
비밀정보 패턴 0건, ZIP 경로 이탈 0건. 이 문서는 초안이며 사람이 확인한 뒤 커밋합니다.

## 후처리 규칙 네 벌이 실제 GPU 회차에서 처음 확인됐다

B(v9·v19·v21·v24)와 D(v3·v4·v7·v8)의 규칙은 지금까지 보관 원응답 재생으로만 쟀다.
이 회차가 같은 규칙을 실제 모델 출력 위에서 처음 돌렸다. 증거 수준이 올라갔다.

`tools/compare_runs.py`로 `colab-1789655036303880754/dev`(b113425)와 대조했다 —
바뀐 셀 93/4800, 대상 밖 17. 도구의 churn 판정: "이번 차이는 관측 범위를 넘는다."

| 항목 | TP | FP | F1 |
| --- | --- | --- | --- |
| v8 | 0→6 | 0→0 | 0.000000→1.000000 |
| v7 | 0→7 | 0→0 | 0.000000→1.000000 |
| v4 | 0→6 | 3→3 | 0.000000→0.800000 |
| v3 | 8→8 | 11→4 | 0.592593→0.800000 |
| v21 | 4→5 | 34→8 | 0.181818→0.526316 |
| v19 | 6→6 | 20→15 | 0.375000→0.444444 |
| v24 | 4→5 | 42→35 | 0.148148→0.208333 |
| v9 | 5→5 | 16→12 | 0.370370→0.434783 |

v19의 20→15는 B가 보관 회차 네 개에서 예측한 값과 같다. 재생으로 잰 규칙이 실제
회차에서 같은 수를 낸 첫 사례다.

대상 밖 17셀은 v5·v10·v11·v13·v15·v17·v23에서 났고 우리 규칙이 건드리지 않는 항목들이다
— 회차 간 모델 churn이다. 규칙의 회귀가 아니다.

## 재생 추정 0.3609와 실측 0.3720의 차이는 churn이다

같은 코드를 보관 원응답으로 재생하면 0.360883975416이었는데 실제 회차는
0.371977547571이다. 차이 0.011은 관측 churn 상한(0.003129)의 3.5배지만 코드 효과가
아니다 — 재생은 `b113425` 회차의 모델 출력을 쓰고 이 회차는 새 출력을 쓴다.
차이의 출처는 모델이 v21·v24에서 TP를 하나씩 더 잡은 것이다(4→5, 4→5).
한 쌍의 Macro F1 차이를 코드 효과로 읽지 않는다.

## 시간 — 이 회차의 본래 목적

| | 값 |
| --- | ---: |
| 모델 로드 | 212.5초 |
| 기본 추론 | 360.1초 |
| 추가 추론 | 106.7초 |
| dev 200건 전체 | 683.3초 (건당 2.33초) |
| 추가 호출 선택 / 폴백 | 105건 / 0건 |
| 모델 정상 응답 | 200/200 |

채점된 `654c556` 회차의 dev는 896.9초였다. -23.8%이고 인수인계가 예측한 675.4초와
7.9초 차이다. 추가 호출이 200건에서 105건으로 준 것이 그대로 재현됐다.

이 감소가 서버에서 재현된다는 보장은 없다. dev 건당 초를 1,853건에 곱해 서버 시간을
추정하지 않는다 — 과거에 실제로 어긋났다. 서버 여유 1,008초가 늘어나는지는 제출해야
안다. 이 회차는 아직 제출하지 않았다.

## 남은 0점 8항목은 그대로다

v10·v11·v12·v14·v15·v16·v18·v20. 전부 C 담당이고 이 회차로 바뀐 것이 없다.
0.40으로 가는 길은 여기에 걸려 있다 — [GPU 회차 대기열](../../docs/tasks/gpu-run-queue.md).

출처. `docs/runs.md` · `reports/runs/colab-1789716500947261692/manifest.json` · `tools/register_run.py`
