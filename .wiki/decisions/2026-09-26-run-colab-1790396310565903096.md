---
scope: project
severity: preference
triggers: ["실행 기록", "run", "runs", "colab", "colab-1790396310565903096", "manifest", "등록"]
domain: 'run-archive'
title: "run: colab-1790396310565903096 결과 ZIP을 실행 기록에 등록"
---

# run: colab-1790396310565903096 결과 ZIP을 실행 기록에 등록

무엇. `artifacts/inbox`의 `colab-results-1790396310565903096.zip`를 `tools/register_run.py`로 등록했습니다.
결과 ZIP의 파일 69개를 `reports/runs/colab-1790396310565903096/`에 텍스트로 풀었고 ZIP 바이너리는
커밋하지 않았습니다. 두 ZIP 해시·코드 커밋·실행 환경·시간·건수·원응답 여부는 같은 폴더의
`manifest.json`에 있습니다. 제출 ZIP 해시의 출처는 `candidate.json`입니다.

왜. 이 등록은 `ea1d8ad` 코드가 NVIDIA A100-SXM4-40GB, vLLM 0.26.0, CUDA 13.0에서 dev-debug 200건, dev 200건, sample 10건을 완주하고 Macro F1 0.756719945813083을 남겼다는 것만 기록하고 대회 서버 제출 성공을 증명하지 않습니다.
수치는 실행이 남긴 파일에서 그대로 옮겼고 등록 과정에서 다시 계산하지 않았습니다.
등록 검사: 결과 ZIP SHA-256 `854731b2…5a6ae`, 제출 ZIP SHA-256 `d8b39d79…4784d`, 파일 69개 UTF-8 해독, 최대 파일 1177KB로 50MB 한도 아래,
비밀정보 패턴 0건, ZIP 경로 이탈 0건. 이 문서는 초안이며 사람이 확인한 뒤 커밋합니다.

판정. 이 회차는 N3(`PRODUCT_ITEMS = ["v10","v11","v12"]`)를 최신 `main`(`de5b873`) 위에
얹어 돌린 것이고, **결과는 기각입니다.** 같은 원응답 위에서 기준 코드와 견주면
Macro 0.793712449043 → 0.759461648555(**−0.034250800489**)이고, 얻은 것은 v12 의 TP
하나뿐인데 v10·v11·v12 의 FP 가 12 → 56(**+44**)으로 늘었습니다. 항목별 TP 감소 0 ·
대상 밖 0셀로 금지선은 지켰지만 점수가 내려갑니다. 추론 898.5초/200건이라 서버 추정이
7,992~8,241초로 한도 7,200초의 111~114% 여서 이겨도 실을 수 없었습니다.

`script.py:106` 의 주석이 적은 과거 회차(`colab-1789724885618578988`)는 +0.003788 ·
FP +38 이었습니다. 그 사이 기준선이 올라 방향이 뒤집혔습니다 — 이 회차가 그 인과를
확정하지는 않습니다.

두 회차를 요청했으나 1회차만 왔으므로 이 코드의 회차 간 churn 은 못 쟀습니다. 다만 위
비교는 **같은 원응답 위의 재생**이라 모델 흔들림이 섞이지 않고, 45셀 중 44셀이 FP 증가
한 방향이므로 판정은 섭니다. 자세한 것은 `reports/team-c/n3-product-r2/RESULT.md` 가
소유합니다.

출처. `docs/runs.md` · `reports/runs/colab-1790396310565903096/manifest.json` ·
`tools/register_run.py` · `reports/team-c/n3-product-r2/RESULT.md`
