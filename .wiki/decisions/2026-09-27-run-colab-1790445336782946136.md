---
scope: project
severity: preference
triggers: ["실행 기록", "run", "runs", "colab", "colab-1790445336782946136", "manifest", "등록"]
domain: 'run-archive'
title: "run: colab-1790445336782946136 결과 ZIP을 실행 기록에 등록"
---

# run: colab-1790445336782946136 결과 ZIP을 실행 기록에 등록

무엇. `artifacts/inbox`의 `colab-results-1790445336782946136.zip`를 `tools/register_run.py`로 등록했습니다.
결과 ZIP의 파일 82개를 `reports/runs/colab-1790445336782946136/`에 텍스트로 풀었고 ZIP 바이너리는
커밋하지 않았습니다. 두 ZIP 해시·코드 커밋·실행 환경·시간·건수·원응답 여부는 같은 폴더의
`manifest.json`에 있습니다. 제출 ZIP 해시의 출처는 `candidate.json`입니다.

왜. 이 등록은 `772ca12` 코드가 NVIDIA A100-SXM4-40GB, vLLM 0.26.0, CUDA 13.0에서 sample 10건, var-01 200건, var-02 200건, var-03 200건, var-04 200건, var-05 200건, var-06 200건을 완주하고 점수 기록 없이 끝났다는 것만 기록하고 대회 서버 제출 성공을 증명하지 않습니다.
수치는 실행이 남긴 파일에서 그대로 옮겼고 등록 과정에서 다시 계산하지 않았습니다.
등록 검사: 결과 ZIP SHA-256 `9166effe…d97f0`, 제출 ZIP SHA-256 `b3d11b9c…a09a1`, 파일 82개 UTF-8 해독, 최대 파일 1041KB로 50MB 한도 아래,
비밀정보 패턴 0건, ZIP 경로 이탈 0건. 이 문서는 초안이며 사람이 확인한 뒤 커밋합니다.

덧. C 의 변동폭 N=6 회차입니다(`reports/team-c/c-variance/`). 같은 코드·같은 입력·같은 설정으로
dev 200건을 6회 돌린 진단 회차이며 제출 후보가 아닙니다. 검증 통과(`dev`·`dev-debug`)를 빼서
`validation.json` 이 없고, 그래서 색인의 Macro 칸에는 단일 값 대신 여섯 통과의 범위를 적었습니다.
여섯 통과 전부 `replay_run.py --verify` 로 바이트 재현됩니다. 판정은
`reports/team-c/c-variance/VERDICT.md` 가 소유합니다 — 관측 6회 범위 0.007287157287, 갈린 셀 0~2.

출처. `docs/runs.md` · `reports/runs/colab-1790445336782946136/manifest.json` · `tools/register_run.py`
