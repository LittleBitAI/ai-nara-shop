---
scope: project
severity: preference
triggers: ["실행 기록", "run", "runs", "colab", "colab-1789886517580784653", "manifest", "등록"]
domain: 'run-archive'
title: "run: colab-1789886517580784653 결과 ZIP을 실행 기록에 등록"
---

# run: colab-1789886517580784653 결과 ZIP을 실행 기록에 등록

무엇. `artifacts/inbox`의 `colab-results-1789886517580784653.zip`를 `tools/register_run.py`로 등록했습니다.
결과 ZIP의 파일 47개를 `reports/runs/colab-1789886517580784653/`에 텍스트로 풀었고 ZIP 바이너리는
커밋하지 않았습니다. 두 ZIP 해시·코드 커밋·실행 환경·시간·건수·원응답 여부는 같은 폴더의
`manifest.json`에 있습니다. 제출 ZIP 해시의 출처는 `candidate.json`입니다.

왜. 이 등록은 `b7ac265` 코드가 NVIDIA A100-SXM4-40GB, vLLM 0.26.0, CUDA 13.0에서 dev 200건, sample 10건을 완주하고 점수 기록 없이 끝났다는 것만 기록하고 대회 서버 제출 성공을 증명하지 않습니다.
수치는 실행이 남긴 파일에서 그대로 옮겼고 등록 과정에서 다시 계산하지 않았습니다.
등록 검사: 결과 ZIP SHA-256 `6193be45…ab3ea`, 제출 ZIP SHA-256 `c1ef2c84…59a15`, 파일 47개 UTF-8 해독, 최대 파일 250KB로 50MB 한도 아래,
비밀정보 패턴 0건, ZIP 경로 이탈 0건. 등록기 초안을 원본 실행 기록과 대조했다. 독립 리뷰는 미실행이다.

후속 진단. dev 프로세스는 883.168초·종료 코드 0·고정 모델 200건 성공이지만, 노트북 check_live의
옛 v13 보호 검사에서 멈췄다. company_size가 소유하는 v13을 바꾼 184번 공고를 잘못 차단했다.
공용/A3 노트북을 수정하고 실제 CSV로 옛 실패·수정 통과를 재현했다. 원본 기록은 바꾸지 않았다.
별도 CPU 복구 채점은 dev **0.5357800832065538**, v10·v18·v20 TP는 0이다.
원응답은 없고 동일 ZIP 반복·무라벨 발화율은 미측정이다. 모델 실행 완료와 노트북 전체 완주는 다르다.
[복구 결과·재개](../../reports/team-c/a3-zero-items/run-1789886517580784653/README.md).

출처. `docs/runs.md` · `reports/runs/colab-1789886517580784653/manifest.json` · `tools/register_run.py`
