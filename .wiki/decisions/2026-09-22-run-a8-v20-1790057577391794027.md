---
scope: project
severity: preference
triggers: ["실행 기록", "run", "runs", "a8", "a8-v20", "a8-v20-1790057577391794027", "1790057577391794027", "회차1", "등록", "별표 주입", "v20", "부재탐지", "기각"]
domain: 'run-archive'
title: "run: a8-v20-1790057577391794027 회차1 등록 — 주입이 v20 을 한 칸도 못 움직였다"
---

# A8 v20 별표 주입 회차1 등록 — v20 바뀐 공고 0건

코드 `e6d9b98`, 순서 `control → a8`, 각 dev 200건. `execution_mode=live`, `status=complete`.
혼합 F1 control 0.591008188119 / a8 0.588468081167. v20 TP/FP/FN 은 두 군 모두 1/4/4 이고
TP·FP·FN 의 공고 ID 집합까지 같다. `compare_runs.py` 의 v20 바뀐 공고 0건, 바뀐 셀 8/4,800 은 전부 대상 밖이다.

기각 근거는 점수가 아니라 관측이다. 제2조와 별표 1 원문 1,568자를 붙이고도
400응답에서 검증된 참여 조항 인용이 0건이다 — `participation_nonnull` 이 두 군 모두 0/200 으로
H4 보관 기준과 같다. `v20_write_1` 5건·`v20_write_0` 0건·`v20_preserve` 195건도 H4 와 같고
write_1 공고 다섯(`PPS-DEV-056`·`064`·`068`·`133`·`144`)까지 그대로다. 개선할 양성 인용이 없었다.
`v20-audit.json` 의 `paired_changes` 4건은 전부 `other` 이고 `verified_quote`·`verified_absence`·
`fallback_only` 는 0건이다. 그중 `PPS-DEV-135` 는 a8 에서 `software_business` yes→no 로
적용성을 잃었다 — 개선이 아니라 손실 방향이며 v20 최종 셀은 안 바뀌었다.

계약은 다 섰다. 입력 30건 해시 일치, 프롬프트 해시가 CPU 원장과 같고 차이는 주입 블록 1,568자뿐,
`budget.json` 추가 축소 0·문서 동일·절단 0, 단계 시간 control 259.825초 / a8 252.497초로 상한 339.178초 안,
`diagnostics.jsonl` 1,618 이벤트에 실제 호출 400회·실패 0·재시도 0·최종 미복구 0,
네 군 OFF/ON 혼합 CSV 바이트 동일. 실행 실패가 아니라 성능 기각이다.

`register_run.py` 는 `colab-results-*.zip` + `submit.zip` 한 쌍만 받아 이 ZIP 에 못 쓰므로
`a5-scope` 선례대로 손으로 등록했다. 회차당 70.2MB 인 `diagnostics.jsonl`·군별 `dev.events.jsonl`
셋은 사용자 결정으로 뺐고 경로·크기·sha256 은 `manifest.json` 의 `raw_responses` 에 있다.
화면 항목은 `<run-id>.control-off`·`.control-on`·`.a8-off`·`.a8-on` 네 개다.
근거: `reports/runs/a8-v20-1790057577391794027/`, `reports/team-c/a8-v20-annex/results.md`.
전체 GPU/서버 점수가 아니다. 운영 채택·대회 제출 없음.
